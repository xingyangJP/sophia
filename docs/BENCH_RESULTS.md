# ベンチマーク履歴

`make bench` の結果を時系列で残す。設定を変えたときに「本当に速くなったか」を後から確認するための記録。

## 読み方の注意（重要）

1. **実行順で結果が最大2倍変わる。** ファンレスのため、後に実行したモデルほど
   熱制限を受けて遅くなる（2026-08-15 12:10 の検証を参照）。
   **異なる計測を比較するときは、実行順と本体の温度状態を揃えること。**
2. **TTFT が `nan` は「本文に到達しなかった」を意味する。** 思考モードのモデルで
   生成上限に達し、思考だけで打ち切られた場合に発生する。
3. 表の TTFT は**本文が出始めるまで**の時間。思考テキストは含まない。
   利用者の体感（何かが表示されるまで）とは別の指標。
4. **`bench.py` が出す「入力処理 tok/s」は偽値。無効。**（2026-08-16 判明）
   同一プロンプトを繰り返すためプレフィックスキャッシュに全命中し、
   実計算がほぼゼロなのに `prompt_eval_count` は全トークンを数えるため、
   割り算した tok/s だけが何十倍にも膨らむ。
   **下記 2026-08-15 の2つの節にある「入力処理 tok/s」列（3,081〜4,171）は
   すべて無効。性能指標として使わないこと。**
   入力処理の真値は 2026-08-16 16:05 の節を見ること（114〜167 tok/s）。
5. **メモリ逼迫が最大の雑音源。** 空きメモリが少ない回はページングで最大4.9倍遅くなる。
   2026-08-16 以降の節にある「空きMB」「ページイン」列を必ず併読すること。
   ページインが数千を超える行は外れ値として扱う。

## 2026-08-15 11:50

- 条件: `num_ctx=8192` / `num_predict=256` / runs=3 / arm
- メモ: 初期構築時のベースライン (Flash Attention有効 / KVキャッシュ q8_0 / 他アプリ通常稼働)

| モデル | プリセット | TTFT(s) | 生成 tok/s | 入力処理 tok/s | 入力tok | 出力tok | 常駐 |
|---|---|--:|--:|--:|--:|--:|--:|
| `sophia-chat` | chat_ja | 15.41 | 13.4 | 3610 | 269 | 256 | 5.6 GB |
| `sophia-chat` | write_ja | 19.42 | 13.2 | 4171 | 310 | 256 | 5.6 GB |
| `sophia-chat` | code | nan | 7.2 | 3700 | 290 | 256 | 5.6 GB |
| `sophia-coder` | chat_ja | 0.21 | 9.0 | 3081 | 196 | 198 | 4.8 GB |
| `sophia-coder` | write_ja | 0.36 | 9.4 | 3522 | 237 | 256 | 4.8 GB |
| `sophia-coder` | code | 0.25 | 9.2 | 3524 | 217 | 256 | 4.8 GB |

## 2026-08-15 12:10 — 熱制限の検証（実行順を逆転）

- 条件: `num_ctx=8192` / `num_predict=256` / runs=3 / chat_ja のみ
- 目的: 上の計測で「8Bのchatが7Bのcoderより速い」という逆転が出たため、
  モデル差か熱制限かを切り分ける

| モデル | プリセット | TTFT(s) | 生成 tok/s | 入力処理 tok/s | 入力tok | 出力tok | 常駐 |
|---|---|--:|--:|--:|--:|--:|--:|
| `sophia-coder` | chat_ja | 0.21 | 14.1 | 3325 | 196 | 198 | 4.8 GB |
| `sophia-chat` | chat_ja | 28.86 | 7.0 | 3563 | 269 | 256 | 5.6 GB |

### 結論

| 実行順 | sophia-coder | sophia-chat |
|---|--:|--:|
| chat先行（11:50） | 9.0（2番目） | 13.4（1番目） |
| coder先行（12:10） | **14.1（1番目）** | **7.0（2番目）** |

**先に走った方が速く、後に走った方が約半分に落ちる。** 熱制限で確定。
モデル間の性能差ではない。`sophia-chat` の TTFT も 15.4s → 28.9s へ悪化した。

→ **連続使用時の実力値は 7〜9 tok/s。** 冷間の13〜14 tok/s を性能指標にしてはならない
（要件 NFR-03b の根拠）。

## 2026-08-16 13:00 — 入力処理（プリフィル）の切り分け計測 ⚠ 無効

> **⚠ この節は KV型の比較として成立していない。信用しないこと。**（2026-08-16 15:43 に判明）
>
> 1. `num_batch` を固定していなかったため、q8_0 側が `-b 512`、f16 側が `-b 1024` で
>    走っている（節末の「ランナーに渡った実引数」に証拠がある）。KV型以外の条件が違う。
>    Ollama は `num_batch` 未指定だと起動時の空きメモリで値を選ぶ。
> 2. `f16 / 4096` の「9 tok/s / 515.5秒」はスワップ枯渇（当時 free_swap=0B）による事故で、
>    **再現しなかった**（同条件の再計測は 122〜138 tok/s）。
>
> 正しい KV型の比較は 2026-08-16 15:59 の節を見ること。

- 計測: `scripts/bench-prompt.py`（`bench.py` とは別物。毎回プロンプト先頭に nonce を入れてプレフィックスキャッシュを壊し、`num_predict=1` で生成を排除している）
- 条件: `sophia-chat` / `num_ctx=8192` / runs=2 × rounds=2 / cooldown=120s / arm
- KV: q8_0 / f16（条件ごとに ollama serve を再起動） / num_batch: 既定
- 実行順: 奇数ラウンドは正順、偶数ラウンドは反転（順序効果の相殺）
- 校正: 1文字あたり 0.462 トークン / テンプレート固定分 244 トークン
- メモ: KV型(q8_0 vs f16)のA/B。num_ctx=8192は本番(Modelfile/実ログのn_ctx_slot)と同値。プロンプト先頭nonceでプレフィックスキャッシュを無効化

**読み方**: 「入力処理 tok/s」は Ollama 報告値（`prompt_eval_count / prompt_eval_duration`）。分子には KV に載ったままの固定プレフィックスも含まれるため、それを引いたものを「実効 tok/s」として併記した。⚠ 付きの行は M3 の理論ピークを超えており、キャッシュ命中の疑いがある。

### 詳細（実行順つき）

| 実行順 | R | KV | batch | 目標tok | 実測tok | 誤差 | 入力処理 tok/s | 実効 tok/s | 幅(min–max) | 処理秒 | swap |
|--:|--:|---|--:|--:|--:|--:|--:|--:|---|--:|--:|
| 1 | 1 | `q8_0` | 既定 | 256 | 293 | +14.5% | 692 ⚠ | 115 | 669–715 | 0.4 | 6274 MB |
| 3 | 1 | `q8_0` | 既定 | 1024 | 1098 | +7.2% | 194 | 151 | 189–198 | 5.7 | 6258 MB |
| 5 | 1 | `q8_0` | 既定 | 4096 | 4767 | +16.4% | 122 | 115 | 115–129 | 39.2 | 6254 MB |
| 7 | 1 | `q8_0` | 既定 | 6000 | 6880 | +14.7% | 98 | 93 | 85–111 | 71.6 | 6501 MB |
| 9 | 1 | `f16` | 既定 | 256 | 292 | +14.1% | 723 ⚠ | 118 | 696–749 | 0.4 | 6642 MB |
| 11 | 1 | `f16` | 既定 | 1024 | 1097 | +7.1% | 197 | 153 | 193–201 | 5.6 | 6642 MB |
| 13 | 1 | `f16` | 既定 | 4096 | 4768 | +16.4% | 9 | 9 | 9–9 | 515.5 | 6607 MB |

### 条件比較（中央値）

| 目標tok | q8_0 / batch=既定 | f16 / batch=既定 | 比(基準=先頭) |
|--:|--:|--:|--:|
| 256 | 692 | 723 | 1.04 |
| 1024 | 194 | 197 | 1.02 |
| 4096 | 122 | 9 | 0.08 |
| 6000 | 98 | - | - |

### 順序効果の確認

| ラウンド | 実行順 | 入力処理 tok/s（全長の中央値） |
|--:|---|--:|
| 1 | q8_0 / batch=既定 → f16 / batch=既定 | 194 |

### ランナーに渡った実引数

- R1 q8_0 / batch=既定: k=q8_0 v=q8_0 fa=on b=512 ub=512 常駐=6.0 GB
- R1 f16 / batch=既定: k=f16 v=f16 fa=on b=1024 ub=1024 常駐=6.6 GB

## 2026-08-16 15:43 — 入力処理（プリフィル）の切り分け計測

- 計測: `scripts/bench-prompt.py`（`bench.py` とは別物。毎回プロンプト先頭に nonce を入れてプレフィックスキャッシュを壊し、`num_predict=1` で生成を排除している）
- 条件: `sophia-chat` / `num_ctx=8192` / runs=2 × rounds=2 / cooldown=90s / arm
- KV: q8_0 / f16（条件ごとに ollama serve を再起動） / num_batch: 512 / 1024
- 実行順: 奇数ラウンドは正順、偶数ラウンドは反転（順序効果の相殺）
- 校正: 1文字あたり 0.503 トークン / テンプレート固定分 238 トークン
- メモ: KV型(q8_0/f16)×num_batch(512/1024)の直交比較。num_ctx=8192は本番(実ログのn_ctx_slot)と同値。目標長4786は実際のOpen WebUI経由のプロンプト長に合わせた。/tokenizeで実トークン数を合わせている。上の13:00の節はnum_batchを固定しておらずq8_0=512/f16=1024で走っており、KV型の比較として成立していない（このスクリプトのランナー実引数ログで判明）

**読み方**: 「入力処理 tok/s」は Ollama 報告値（`prompt_eval_count / prompt_eval_duration`）。分子には KV に載ったままの固定プレフィックスも含まれるため、それを引いたものを「実効 tok/s」として併記した。⚠ 付きの行は M3 の理論ピークを超えており、キャッシュ命中の疑いがある。

### 詳細（実行順つき）

| 実行順 | R | KV | batch | 目標tok | 実測tok | 誤差 | 入力処理 tok/s | 実効 tok/s | 幅(min–max) | 処理秒 | 空きMB | ページイン |
|--:|--:|---|--:|--:|--:|--:|--:|--:|---|--:|--:|--:|
| 1 | 1 | `q8_0` | 512 | 1024 | 1022 | -0.2% | 208 | 160 | 208–209 | 4.9 | 2138 | 12 |
| 3 | 1 | `q8_0` | 512 | 4786 | 4792 | +0.1% | 108 | 100 | 90–127 | 45.6 | 1962 | 856 |
| 5 | 1 | `f16` | 512 | 1024 | 1023 | -0.1% | 100 | 77 | 97–103 | 10.2 | 1521 | 455 |
| 7 | 1 | `f16` | 512 | 4786 | 4791 | +0.1% | 64 | 61 | 64–64 | 74.4 | 1336 | 30152 |
| 9 | 1 | `q8_0` | 1024 | 1024 | 1022 | -0.2% | 200 | 153 | 199–201 | 5.1 | 2541 | 595 |
| 11 | 1 | `q8_0` | 1024 | 4786 | 4792 | +0.1% | 98 | 84 | 67–128 | 54.3 | 2420 | 2301 |
| 13 | 1 | `f16` | 1024 | 1024 | 1022 | -0.2% | 204 | 157 | 203–206 | 5.0 | 2058 | 168 |
| 15 | 1 | `f16` | 1024 | 4786 | 4791 | +0.1% | 130 | 123 | 122–138 | 36.9 | 1742 | 1706 |
| 17 | 2 | `f16` | 1024 | 4786 | 4791 | +0.1% | 68 | 58 | 47–89 | 78.3 | 1973 | 2748 |
| 19 | 2 | `f16` | 1024 | 1024 | 1022 | -0.2% | 173 | 133 | 171–175 | 5.9 | 1427 | 94 |
| 21 | 2 | `q8_0` | 1024 | 4786 | 4793 | +0.1% | 69 | 46 | 31–108 | 99.5 | 1965 | 17941 |
| 23 | 2 | `q8_0` | 1024 | 1024 | 1020 | -0.4% | 159 | 122 | 158–160 | 6.4 | 2012 | 316 |
| 25 | 2 | `f16` | 512 | 4786 | 4791 | +0.1% | 138 | 131 | 132–144 | 34.9 | 1666 | 26998 |
| 27 | 2 | `f16` | 512 | 1024 | 1020 | -0.4% | 212 | 162 | 207–216 | 4.8 | 1416 | 474 |
| 29 | 2 | `q8_0` | 512 | 4786 | 4791 | +0.1% | 108 | 96 | 81–136 | 47.3 | 2720 | 1913 |
| 31 | 2 | `q8_0` | 512 | 1024 | 1022 | -0.2% | 211 | 162 | 211–212 | 4.8 | 2409 | 156 |

### 条件比較（中央値）

| 目標tok | q8_0 / batch=512 | f16 / batch=512 | q8_0 / batch=1024 | f16 / batch=1024 | 比(基準=先頭) |
|--:|--:|--:|--:|--:|--:|
| 1024 | 210 | 156 | 180 | 189 | 0.74 / 0.86 / 0.90 |
| 4786 | 108 | 101 | 84 | 99 | 0.93 / 0.77 / 0.91 |

### 順序効果の確認

| ラウンド | 実行順 | 入力処理 tok/s（全長の中央値） |
|--:|---|--:|
| 1 | q8_0 / batch=512 → f16 / batch=512 → q8_0 / batch=1024 → f16 / batch=1024 | 119 |
| 2 | f16 / batch=1024 → q8_0 / batch=1024 → f16 / batch=512 → q8_0 / batch=512 | 148 |

### ランナーに渡った実引数

- R1 q8_0 / batch=512: k=q8_0 v=q8_0 fa=on b=512 ub=512 常駐=6.0 GB
- R1 f16 / batch=512: k=f16 v=f16 fa=on b=512 ub=512 常駐=6.5 GB
- R1 q8_0 / batch=1024: k=q8_0 v=q8_0 fa=on b=1024 ub=1024 常駐=6.1 GB
- R1 f16 / batch=1024: k=f16 v=f16 fa=on b=1024 ub=1024 常駐=6.6 GB
- R2 f16 / batch=1024: k=f16 v=f16 fa=on b=1024 ub=1024 常駐=6.6 GB
- R2 q8_0 / batch=1024: k=q8_0 v=q8_0 fa=on b=1024 ub=1024 常駐=6.1 GB
- R2 f16 / batch=512: k=f16 v=f16 fa=on b=512 ub=512 常駐=6.5 GB
- R2 q8_0 / batch=512: k=q8_0 v=q8_0 fa=on b=512 ub=512 常駐=6.0 GB

## 2026-08-16 15:59 — 入力処理（プリフィル）の切り分け計測

- 計測: `scripts/bench-prompt.py`（`bench.py` とは別物。毎回プロンプト先頭に nonce を入れてプレフィックスキャッシュを壊し、`num_predict=1` で生成を排除している）
- 条件: `sophia-chat` / `num_ctx=8192` / runs=1 × rounds=3 / cooldown=90s / arm
- KV: q8_0 / f16（条件ごとに ollama serve を再起動） / num_batch: 512
- 実行順: 奇数ラウンドは正順、偶数ラウンドは反転（順序効果の相殺）
- 校正: 1文字あたり 0.503 トークン / テンプレート固定分 238 トークン
- メモ: KV型のみの再確認。全ての計測の直前に必ず90s冷却を入れる構成（runs=1×rounds=3）。本計測(15:01の節)では同一条件の2回目が連続実行になり、1.1〜3.5倍遅くなる現象が出たため、その影響を除いて比べ直したもの

**読み方**: 「入力処理 tok/s」は Ollama 報告値（`prompt_eval_count / prompt_eval_duration`）。分子には KV に載ったままの固定プレフィックスも含まれるため、それを引いたものを「実効 tok/s」として併記した。⚠ 付きの行は M3 の理論ピークを超えており、キャッシュ命中の疑いがある。

### 詳細（実行順つき）

| 実行順 | R | KV | batch | 目標tok | 実測tok | 誤差 | 入力処理 tok/s | 実効 tok/s | 幅(min–max) | 処理秒 | 空きMB | ページイン |
|--:|--:|---|--:|--:|--:|--:|--:|--:|---|--:|--:|--:|
| 1 | 1 | `q8_0` | 512 | 4786 | 4791 | +0.1% | 140 | 133 | - | 34.3 | 2828 | 3456 |
| 2 | 1 | `f16` | 512 | 4786 | 4792 | +0.1% | 151 | 144 | - | 31.7 | 2378 | 446 |
| 3 | 2 | `f16` | 512 | 4786 | 4792 | +0.1% | 147 | 140 | - | 32.5 | 2213 | 511 |
| 4 | 2 | `q8_0` | 512 | 4786 | 4792 | +0.1% | 108 | 102 | - | 44.5 | 2300 | 7209 |
| 5 | 3 | `q8_0` | 512 | 4786 | 4792 | +0.1% | 139 | 132 | - | 34.5 | 2429 | 64 |
| 6 | 3 | `f16` | 512 | 4786 | 4792 | +0.1% | 147 | 140 | - | 32.5 | 1910 | 404 |

### 条件比較（中央値）

| 目標tok | q8_0 / batch=512 | f16 / batch=512 | 比(基準=先頭) |
|--:|--:|--:|--:|
| 4786 | 139 | 147 | 1.06 |

### 順序効果の確認

| ラウンド | 実行順 | 入力処理 tok/s（全長の中央値） |
|--:|---|--:|
| 1 | q8_0 / batch=512 → f16 / batch=512 | 145 |
| 2 | f16 / batch=512 → q8_0 / batch=512 | 128 |
| 3 | q8_0 / batch=512 → f16 / batch=512 | 143 |

### ランナーに渡った実引数

- R1 q8_0 / batch=512: k=q8_0 v=q8_0 fa=on b=512 ub=512 常駐=6.0 GB
- R1 f16 / batch=512: k=f16 v=f16 fa=on b=512 ub=512 常駐=6.5 GB
- R2 f16 / batch=512: k=f16 v=f16 fa=on b=512 ub=512 常駐=6.5 GB
- R2 q8_0 / batch=512: k=q8_0 v=q8_0 fa=on b=512 ub=512 常駐=6.0 GB
- R3 q8_0 / batch=512: k=q8_0 v=q8_0 fa=on b=512 ub=512 常駐=6.0 GB
- R3 f16 / batch=512: k=f16 v=f16 fa=on b=512 ub=512 常駐=6.5 GB

## 2026-08-16 16:05 — プロンプト長と入力処理速度（長尺）

- 計測: `scripts/bench-prompt.py` / `sophia-chat` / `num_ctx=8192` / KV `q8_0` / `num_batch=512` 固定
- 条件: runs=1 × rounds=2 / **各計測の直前に90秒冷却** / コールドスタート
- 実行順: R1 → R2 の順に同一条件を2回（順序効果の対象なし。長さ依存だけを見るため）
- 生データ: `long.json`（空きメモリ 2,352 / 2,423 MB、スワップ 5.85 / 5.80 GB）
  ※ このファイルはリポジトリに保存されていない（計測時の作業ディレクトリに出力したまま）。
  本節の表が残っている唯一の記録であり、**再検証するには測り直しが必要**。
- 目的: 7,688トークンまで伸ばして、速度がプロンプト長にどう依存するかを確定させる

### プロンプト長と入力処理時間（冷却後の最良値・llama-server 自身の報告）

各点の出典: 273 と 7,688 は本節、1,022 は 15:43 の節（実行順31）、4,792 は 15:59 の節（実行順1）。
いずれも直前に冷却を挟んだ q8_0 / batch=512 の値。

| 入力トークン | 実計算トークン | 処理秒 | tok/s | 1トークンあたり |
|--:|--:|--:|--:|--:|
| 273 | 273 | 1.6 | 167 | 5.98 ms |
| 1,022 | 789 | 4.8 | 163 | 6.13 ms |
| 4,792 | 4,561 | 34.3 | 133 | 7.52 ms |
| 7,688 | 7,457 | 65.2 | 114 | 8.74 ms |

**長いほど1トークンあたりが高くつく。総時間はプロンプト長に対して超線形。**

`tok/s` と `1トークンあたり` の分母は **`実計算トークン`** であって `入力トークン` ではない
（273の行以外は、テンプレート冒頭の約231トークンが KV に残っていて計算されていない）。
入力トークンで割ると 1,022÷4.8=213 のように別の値になるので注意。
この表を引用する場合は必ず `実計算トークン` 列も一緒に持っていくこと。

### 1リクエスト内の区間速度（7,688トークンのプリフィル1本）

同一リクエスト・同一温度・同一設定の中で速度が単調に落ちていく。
`logs/bench-prompt-ollama.log` の `prompt processing` 行から512トークン刻みで算出。

| 累積tok | 1536 | 2048 | 2560 | 3072 | 3584 | 4096 | 4608 | 5120 | 5632 | 6144 | 6656 | 7168 |
|---|--:|--:|--:|--:|--:|--:|--:|--:|--:|--:|--:|--:|
| R1 区間 tok/s | 252 | 157 | 142 | 132 | 125 | 119 | 113 | 107 | 102 | 99 | 96 | 91 |
| R2 区間 tok/s | 166 | 156 | 140 | 129 | 119 | 112 | 106 | 100 | 98 | 93 | 89 | 87 |

文脈2kの地点で157 tok/s、7kの地点で91 tok/s。**プロンプト長の効果であり、設定では消せない。**

### 4,786トークン（実際の Open WebUI のプロンプト長）が何秒かかるか

| 条件 | 処理秒 | 出典 |
|---|--:|---|
| 最良（冷却後・ページングなし・f16） | **31.7** | 15:59 実行順2 |
| 最良（冷却後・q8_0 ＝現行設定） | **34.3〜34.5** | 15:59 実行順1/5 |
| 冷却を挟んだ計測16件の中央値 | 36.3 | 15:43 + 15:59 |
| 直前に同じ処理を走らせた場合（連続2回目）の中央値 | 65.3 | 15:43 の8ペア |
| 実測最悪 | 154.5 | 15:43 系列（ページング） |

---

## 2026-08-16 まとめ — 入力処理の調査結果

**当初の問題設定「入力処理速度が計測ごとに25倍違う（3,610 vs 140 tok/s）」は、
前提が誤っていた。** 検証した4つの仮説の結果:

| # | 仮説 | 結果 |
|---|---|---|
| 1 | `OLLAMA_KV_CACHE_TYPE=q8_0` が Metal で入力処理を大幅に遅くしている | **否定。** f16 が約6%速いだけで、機体のばらつき（同一条件で最大4.9倍）に埋もれる。25倍の説明にはならない |
| 2 | 512トークンごとにKVキャッシュを捨てて再計算している | **否定。** `cached n_tokens` は512ずつ**増えて**おり、チャンク処理の記帳。実計算 4,560 = 4,793 − 233（キャッシュ命中）が厳密に一致する |
| 3 | 140 tok/s が異常に遅い | **否定。** これがこの機体の素の実力値。異常なのは 3,610 tok/s の側で、プレフィックスキャッシュ命中を入力処理として計上した偽値（59 TFLOP/s 相当＝理論ピークの9倍） |
| 4 | `num_batch` 512 が律速している | **判定不能。** ばらつきが条件差より大きく、512 と 1024 を区別できなかった |

**残った真の原因は、設定ではなくプロンプト長そのもの。**
Open WebUI が「こんにちは」の一言に4,786トークン送っており、
そのうち約4,550トークン（95%）がビルトインツール定義の自動注入だった。
入力処理は正常に約140 tok/s で動いており、**4,786トークンなら32〜35秒かかるのが正しい挙動**。

→ 対処は `.env` と TUNING.md 第2章へ反映済み。
→ 対策後の再計測では、速度を比べる前に必ず `logs/ollama.log` の
  `task.n_tokens` を見て入力長が実際に減ったことを確認すること。

### この計測で判明した、計測手順そのものの問題

1. `scripts/bench.py` の「入力処理 tok/s」は偽値（上記「読み方の注意」4）
2. `num_batch` を固定しないと Ollama が起動ごとに違う値を選ぶ
   （未固定の起動6回で `-b 512` が5回、`-b 1024` が1回。空きメモリ最大の回だけ1024）
3. メモリ逼迫が熱より大きい雑音源。1回の計測中にページアウト90,104ページ（約1.4GB）が
   発生したブロックがあり、そのブロックだけ全長で速度が半分になった
4. 冷却なしの連続実行で中央値1.55倍遅くなる（8ブロックの比: 1.00〜3.47倍）。
   熱とページングの両方が混ざっており、**どちらが主因かは切り分けていない**

### まだ測っていないこと

- `.env` に追加した `ENABLE_MEMORIES` / `ENABLE_CALENDAR` / `ENABLE_AUTOMATIONS` /
  `ENABLE_NOTES` / `ENABLE_PERSISTENT_CONFIG=False` の効果（**未検証**）。
  `make restart`（`make up` は起動済みだと何もしないので不可）してから
  `task.n_tokens` が 4,786 から減るかを確認する。期待値 約1,600 は計算値で±数百の誤差あり
- モデル capability「Builtin Tools」を off にしたときの実測（期待値: 約231トークン）
- コンテキストシフト（`num_ctx=8192` 超過時）の崖が実際に起きるか
- メモリを空けたクリーン状態での再計測（`num_batch` の判定はこれをやらないと出ない）

---

## 2026-08-17 08:45 — アプリ（MLX / Omoikane 8B）初計測 ⚠ 2点のみ・Debug

**アプリ側で初めて取れた数字。** これまでの本文書の記録はすべて Ollama + GGUF であり、
**この節だけが別系統**である。2点しか無く、しかも計測条件が悪い（下記）。
**基準値ではない。**

### 条件

| 項目 | 値 |
|---|---|
| 経路 | アプリ（`Sophia.app`）/ MLX Swift |
| モデル | `mlx-community/Qwen3-8B-4bit`（表示名 `Omoikane 8B v1.0`）|
| ビルド | **Debug ビルド（デバッガは非接続）** ⚠（落とし穴15 は外せと明記） |
| 思考モード | **ON** |
| 自己認識（FR-23） | **ON**（system 3行 / 概算+71トークン） |
| 試行 | **同一セッション内の連続2往復のみ** |
| 起動時スワップ | **5,120M 中 4,056M 使用済み**（`vm.swapusage`）⚠ |
| 取得方法 | `make app-stats` → `logs/mlx-stats.log` |

### 結果 — 2往復で何が起きたか

| 項目 | 1往復目 | 2往復目 | 変化 |
|---|--:|--:|---|
| 入力トークン | 104 | 156 | +50% |
| 出力トークン | 166 | 383 | +131% |
| **プリフィル速度** | **123.26 tok/s** | **13.39 tok/s** | **9.2倍 悪化** |
| プリフィル時間 | 0.84秒 | 11.65秒 | 13.9倍 |
| **TTFT**（最初の1文字） | **0.97秒** | **11.87秒** | **12.2倍 悪化** |
| TTFR（**本文**の1文字目） | 7.71秒 | 28.36秒 | 3.7倍 |
| **デコード速度** | 19.46 tok/s | 19.21 tok/s | **ほぼ不変（−1.3%）** |
| 合計 | 9.42秒 | 31.66秒 | — |
| ピークメモリ | 4,615.61 MB | 4,656.19 MB | +40MB |

### この計測の最大の発見 — プリフィルだけが崩れる

**デコードは無傷（19.46 → 19.21 tok/s）なのに、プリフィルだけが崩れている。**
全体的な熱制限なら**デコードも道連れになるはず**で、そうなっていない。

> **「9.2倍」と「13.9倍」を別々の証拠として数えないこと**（初版はそう書いていた。撤回する）。
> **`prefill_tps` は独立した測定値ではなく `in ÷ prefill_s` の再掲**である
> （MLX 側で `promptTokenCount / promptTime` として計算される）。
> 実測値は `prefill_s` の 0.84秒 → 11.65秒 の1つだけで、残りはその言い換えにすぎない。

#### 決定的な数字 — 2往復目は「計算していない」

1トークンあたりの単価に直すと、原因の性質が絞れる。

| | プリフィル | デコード |
|---|--:|--:|
| 1往復目 | **8.1 ms/tok** | 51.4 ms/tok |
| 2往復目 | **74.7 ms/tok** | 52.1 ms/tok |

**2往復目は、プリフィルの1トークン単価がデコードを上回っている。**
プリフィルは重みの読み出しを156トークンで償却できるぶん、**計算律速なら単価はデコードより
必ず安くなる**（1往復目はまさにそうで、6.4倍安い）。**それが逆転しているということは、
11.65秒の大半は演算ではない。**

**入力長そのものも説明にならない。** 104→156トークンは1.5倍でしかない。

> **⚠️ この節の初版は「ピークメモリも+40MBしか動いていないのでメモリ不足では説明がつかない」と
> 書いていた。これは誤りだったので撤回する。**
> `peak_mb` は生成ごとに `MLX.Memory.peakMemory = 0` でリセットされる
> **「その生成中のピーク*アロケーション*量」**である（`MLXEngine.swift:290`）。
> MLX の会計はアロケーション量であって、**そのページが物理RAMにあるかスワップにあるかを
> 一切知らない。4.6GB 全部がスワップに出ていても値は変わらない。**
> したがって `peak_mb` が動かないことは、**KVの膨張は否定できるが、ページアウトは否定できない。**
> **residency を測るには OS 側（`phys_footprint` / ページイン数）を見るしかない。**

#### 有力仮説: 待機中に重みがページアウトされ、プリフィルが読み戻しの代金を払った

**コード調査で、この仮説の前提条件がすべて満たされていることを確認した。**

| 確認したこと | 根拠 |
|---|---|
| **重みは wired されていない** ─ macOS から見て追い出せる普通のページ | `generate(...)` の `wiredMemoryTicket` が既定 nil。MLX の公式ドキュメントは `kind: .reservation` を「モデルの重みのような長寿命メモリ用」と明記 |
| モデルは往復をまたいで保持されている（**都度ロードではない**） | `MLXEngine.swift:39` の `container`、同 `:167` で同一モデルなら即 return |
| 往復の合間に `clearCache()` は呼ばれない | 呼び出し元はモデル切替時のみ（`MLXEngine.swift:177`） |
| **`prefill_s` の計測窓が「待機後に4.6GB全体へ最初に触る一回」と正確に一致する** | MLX 側 `promptPrefillTime` はプロンプト全体の全層 forward。`decode_s` は2トークン目以降＝すでに常駐 |
| メモリ圧への反応が一切ない | memory pressure ハンドラ・背景遷移時の解放とも0件 |
| `cacheLimit` が 20MB と小さい | `ChatOptions.swift:139`。1往復目終了時に KV と中間バッファは**OSへ返る**。2往復目はそれを取り直す |

**`prefill_s` に別の作業が混入している線は潰れた。** 計測窓に含まれるのは
プロンプト全体のフォワードパスだけで、**モデルのロード・チャットテンプレート適用・
トークナイズ・KVキャッシュ確保はいずれも窓の外**である。
独立した裏付けがログ自身にある ─ `ttft_s` の起点は `prefill` より前なので、その差が
窓外の全コストの上限になる:

| | ttft_s | prefill_s | 差＝窓外の全コスト |
|---|--:|--:|--:|
| 1往復目 | 0.97 | 0.84 | **0.13秒** |
| 2往復目 | 11.87 | 11.65 | **0.22秒** |

**窓の外は合計0.22秒以下。11.65秒は本当にプロンプトのフォワードパスの壁時計である。**

**桁の整合（計算であって証拠ではない）**: 1往復目と同じ123.26 tok/s なら156トークンは1.27秒。
実測11.65秒との差は約10.4秒。4.6GB を10.4秒で戻すと **約440 MB/s** ─
圧縮スワップの伸長＋NVMe 読みとしてありうる桁である。
**ただし他プロセスとのメモリ争奪でも同じ桁が出るため、これだけでは決められない。**

#### 消えた対抗仮説

| 対抗仮説 | 判定 | 根拠 |
|---|---|---|
| サーマル／クロック低下 | **ほぼ反証** | decode が −1.3% しか落ちていない。機体が絞られたなら decode も落ちる |
| プリフィルのチャンク境界差 | **反証** | 既定ステップ幅512に対し104も156も未満で同一経路 |
| 入力長の増加そのもの | **反証** | 1.5倍の入力で、この劣化は説明できない |
| **モデルを都度ロードし直している** | **反証** | `container` が保持されている（上表） |
| **計測窓への別作業の混入** | **反証** | 窓外の全コストが0.22秒以下（上記） |
| **計算律速（プリフィルが本当に重い）** | **反証** | 2往復目はプリフィル単価がデコードを上回っており、原理的にありえない |
| **入力長ごとのカーネル遅延コンパイル** | **弱まった** | **1往復目こそ全プリフィル中の初回**で JIT を払う側なのに、123 tok/s で速い |

#### 残る候補

| # | 仮説 | 状態 | 見分け方 |
|---|---|---|---|
| 1 | **待機中のページアウト** | **本命** | 手順は下記。ページイン数が跳ねれば確定 |
| 2 | KVを持ち越さず毎回再プリフィル | **確定だが主因ではない** | 下記。**11.65秒の11%しか説明しない** |
| 3 | 他プロセスとの CPU/メモリ争奪が壁時計に乗った | 未確認 | `promptTime` は `Date` ベースの壁時計なので乗りうる。**静かな状態での再測定で分かる** |
| 4 | ~~Debug ビルドの増幅~~ | **原理的に却下** | **ビルド構成は1往復目と2往復目で同一。** 両方を一様に遅くすることはあっても、2往復目だけを9倍にはできない。デバッガも非接続（`make app-stats` は `open -n` 経由）。Release で取り直すことは必要だが、**この差の説明にはならない** |

#### KVキャッシュは持ち越されていない（確定）── ただし主因ではない

`MLXEngine` が呼ぶ `generate(input:parameters:context:components:)` は **`cache:` を渡しておらず**、
MLX 側は毎回 `model.newCache(...)` で**全36層ぶんの新規キャッシュ**を作る。
エンジンは `[KVCache]` も `LMOutput.State` も保持していない。
`ChatViewModel` は毎ターン system + 完了済みの全ターンを再送している
（**思考テキストは再送していない** ─ 入力トークンの増分52がその内訳と一致することで裏が取れた）。

**しかしこれは今回の主因ではない。** 156トークンを1往復目のレート（123.26 tok/s）で
全部再プリフィルしても1.27秒で、**11.65秒の11%にしかならない。**

> **とはいえ、放置してよい問題ではない。**
> 毎ターン全再プリフィルは、会話が伸びるほど**二乗で効く実在の税**である。
> 直す形は `MLXEngine` actor に `[KVCache]` + `LMOutput.State?` + トークン台帳を持たせ、
> `generate(input:cache:state:...)` へ渡すこと。
> **`ChatSession` をそのまま使ってはいけない** ─ 中断したターンが履歴に残らないという
> FR-02 の要件と衝突するため、台帳は自前で持つ必要がある。

#### 併せて見つかった構造的な無駄 — `lm_head` が全位置に適用されている

MLX の Qwen3 実装は最終隠れ状態の**全位置**に `lm_head` を掛けており、
プロンプトのフォワードで `[1, L, 151936]` のロジットを実体化する
（bf16 で 104トークン→約31.6MB / 156トークン→約47.4MB）。**使うのは最終行だけである。**
どちらも `cacheLimit` の20MBを超えるためバッファキャッシュに載らず、**毎回 Metal から新規確保**になる。

**これは上流ライブラリの実装であってアプリ側のコードではない**ので、すぐには直せない。
ただし**会話が伸びるほど線形に効く**ため、記録しておく。

#### ⚠️ 直す前に、まず再現するかを確かめること

**この11.65秒は、アプリの性質ではなく「そのときの機体の状態」を測った数字である可能性が相当ある。**
計測はスワップが 5,120M 中 4,056M 埋まった機体で、**並行作業の最中に**取られている。
**静かな状態で再測定して再現しなければ、直すべきものは何もない。**

#### 白黒つける最短手順（アプリを1行も変えずにできる）

**手順0 — 最も安く効く1手: まったく同じプロンプトを2回続けて送る。**

**入力トークン数が同一**なので、トークン数・会話長・入力長といった変数が全部消える。
**2回目が速ければ、原因はトークン数でも会話長でもなく「初回に触る」側だと確定する。**
（会話履歴が積み上がらないよう、2回目は新しい会話で送るか、同じ問いを繰り返す）

**手順1 — 対照実験。** 同じセッションで3パターンを測る。

- (a) 間を空けず2連続で送信（合間1〜2秒）
- (b) 合間に3〜5分待ってから2往復目
- (c) 待機中に他プロセスのメモリ需要を作る／作らない

**(a) で遅くならず (b) で遅くなれば、ページアウト説を強く支持。** (a) でも遅ければ別原因。
**(a)(b) とも遅くならないなら、当日の並行作業による争奪が主因だったことになる。**

**手順2 — OS 側のページイン数を差分で取る。** 別ターミナルで、
1往復目直後 / 2往復目直前 / 2往復目直後 の3点で:

```
vm_stat | grep -E "Pageins|Decompressions|Swapins"
footprint -p $(pgrep -x Sophia)
```

- 2往復目のプリフィル中に **Pageins / Decompressions が30万〜120万ページ規模で増える**なら
  **確定**（4.6GB ÷ 16KB ≈ 288Kページ、÷4KB ≈ 1.15Mページ）
- ほとんど増えないのに11秒かかるなら**反証**
- `phys_footprint` が待機中に落ちて2往復目で戻れば、これも決定的

> **Instruments は使わないこと。** この機体では観測行為自体が対象を壊す。

**手順3 — アプリに足すなら**（要ビルドなので後回し）:
`MLX.Memory.snapshot()` を足しても**無駄**（residency を測らないため）。
`task_info(mach_task_self_, TASK_VM_INFO, ...)` の `phys_footprint` と、
`TASK_EVENTS_INFO.pageins`（**プロセス内で取れる。sudo 不要**）を
**prefill 直前・直後**で出す。これなら1往復で白黒がつく。
`GenerationStats` は struct なのでフィールド追加で既存コードは壊れない。

#### 計測ハーネスの形（調査済み）

**GUI を介さずエンジンを N 往復駆動することはできる。ただし XCTest に限る。**

| 方式 | 可否 | 理由 |
|---|---|---|
| 単発の `swift script` | **不可** | `swift build` では `.metallib` が生成されず MLX 推論が動かない。既存の `scripts/test-*.swift` は MLX 非依存の文字列テスト専用 |
| **`SophiaTests` に probe を足す** | **可** | テストターゲットの `TEST_HOST` が `Sophia.app` で、テストバンドル内に `default.metallib` が実在する。**モデル重みも同じサンドボックスコンテナにあるので再ダウンロード不要。** タイムアウトも無効化されており900秒の待ちを入れられる |

推奨する形は **`build-for-testing` を1回 → 条件ごとに `test-without-building`**。
条件は `.xctestrun` を複製して環境変数を書き換えて注入する。
**ビルドが計測に混ざらない**（`app-stats` をビルドに依存させなかったのと同じ理由）、
出荷物が汚れない、CLI 実行なのでデバッガが付かない。
実装は新規2ファイル＋既存3ファイルへの軽微な追加で、**実装約5時間＋実行3〜4時間**（待ち時間が支配的）。

> **ただし、最初にやるべきは実装ゼロの30分スモークである。**
> 別窓で `vm_stat` / `footprint` を2秒ごとに記録しながら、従来どおり `make app-stats` で2往復する。
> **2往復目のプリフィル区間に Pageins のスパイクが乗るかを見るだけ。**
> ここで見えれば5時間の実装に入る前に本命がほぼ確定し、作るものを絞れる。
> **そして何より、9.2倍がそもそも再現するかをここで確かめられる。**

**実験計画の全体**（E1〜E7 の条件と判定基準）は別途スクラッチに置いてある。要点:

| # | 条件 | 判定基準 |
|---|---|---|
| E1 | 同一入力を待ち0秒で5回 | 1回目だけ遅い→初回コスト / 全部同じ→初回コスト却下 |
| E3 | 待ち **0/30/60/300/900秒** | プリフィルがアイドル時間と単調増加、かつ pageins 大 → **ページアウト説を確定** |
| E4 | 待ち中に8GBを能動注入 | 注入時だけ崩れる → **因果として確定**（E3 の相関より強い） |
| E5 | `WiredMemoryTicket` で重みを wire | 崩れが消える → **確定と恒久対策が同時に決まる** |
| E7 | `cacheLimit` 20MB vs 512MB | 512MB で消えるなら**別の新原因** |

**これは A1 の残作業ではなく、体感速度に直結する最優先の調査項目である。**

### NFR への影響

- **NFR-03（1秒以内に何かが出る）は2往復目で破れている。** 1往復目 0.97秒 に対し 2往復目 11.87秒。
  **「初回だけ条件を満たす」のは満たしていないのと同じ**である。
- **NFR-03 の「入力約170トークンまで」という閾値の根拠も揺らぐ。** あれは Ollama からの内挿値。
  1往復目は104トークンで既に0.97秒（[REQUIREMENTS.md](REQUIREMENTS.md) NFR-03 の但し書き）。
- メモリの**アロケーション**は見積り約5.4GB に対しピーク4,656MB で収まっている。
  ただし短い2往復の値であり、**これは常駐量ではない**（上の但し書き）。
  NFR-04（アプリ自体の常駐500MB以内）の判定には使えない。

### 参考: Ollama との比較（条件が違うので目安）

デコード 19.2〜19.5 tok/s に対し、Ollama は冷間13.4 / 連続7.0（2026-08-15）。
**MLX のデコードは速く、しかも2往復目でも落ちていない。**
Ollama で見えた「連続使用で半減」がデコード側には出ていない点は収穫である。
ただし **Debug ビルドでこの値**であり、条件を揃えた比較ではない。

### 信用してはいけない値

**`think_tok`（296 / 288）は壊れている。** 1往復目は出力全体が166トークンなのに思考が296 ─
**原理的にありえない。** 思考が英語のとき「0.5 tok/文字」の概算係数が約2倍過大に出る
（[DESIGN.md](DESIGN.md) リスク20）。**思考量の議論にこの数字を使わないこと。**

### 仮説1（ページアウト）が正しかった場合の対処案

**安い順。どれも「測ってから」で、先回りして入れないこと。**

| # | 対処 | 性質 | 但し書き |
|---|---|---|---|
| 1 | **生成中だけ wired チケットを渡す**（`generate(..., wiredMemoryTicket:)`） | 設定 | ページアウト自体は防げないが、**プリフィル中の再ページアウト往復（スラッシング）は防げる**可能性 |
| 2 | **KV量子化 / `maxKVSize`** | 設定 | `ChatOptions.swift` に口はあるが現状すべて nil。ワーキングセットを減らせば追い出される確率が下がる |
| 3 | `cacheLimit` を 20MB → 256MB 程度へ | 設定 | **両方向に働く。** バッファをOSに返さず済む一方、常駐が増えてページアウトを悪化させうる。**必ず測ってから** |
| 4 | 重みに reservation チケットを張る | 設計 | 公式の道具だが、**16GB機で4.6GBを wired にすると他が死ぬ。** 全量は選ばず、部分的な値で測るのが先 |
| 5 | keep-warm（アイドル中に軽い forward） | 設計 | **VISION の「1/1000のエネルギー」と真正面から衝突する。** 採用するなら衝突を測って正当化が要る |
| 6 | prompt cache / KV の持ち越し | 設計 | 会話が伸びるほどプリフィルが線形に増える問題には効く。**ただし今回の9.2倍は説明しない** |
| 7 | **「直す」のではなく「見せる」** | UI | プリフィル進捗は**既に配線済み**（`reportsPrefillProgress: true`）。11秒の無言だけは今すぐ消せる |

### 次に取ること（優先順）

1. **静かな状態で再現するかを確かめる。** 再現しなければ直すべきものは無い
2. **プリフィル崩れの切り分け**（上の手順1・2）。**これが最優先。**
   3往復以上を1セッションで回し、往復ごとに `vm_stat` のページイン数を併記する
2. **Release ビルド・デバッガ非接続での取り直し**（これが基準値になる）
3. 冷間と連続使用の別
4. `SOPHIA_SYSTEM_PROMPT=0` との A/B で自己認識の実トークン増分を確定
   （概算+71 を実測で置き換える。[DESIGN.md](DESIGN.md) 4.8節）
5. 思考モード OFF での TTFR（思考ONの代償を数字にする）

> **計測時の注意（実地で踏んだもの）**
> - **`make app-run` では `[STATS]` が1バイトも残らない。** `open` 経由は fd 0/1/2 が
>   /dev/null に繋がる。`log stream` でも拾えない（生の `write(2)`）。必ず `make app-stats`。
> - **起動中のアプリがあると `open` は新しいビルドを起動しない。** 計測前に `pkill` すること。
>   修正前のバイナリを計測して誤診する事故を実際に1度起こしている。
> - **Ollama と同時に動かさない。** 16GB機では両方が載ると数字が壊れる。
> - **スワップの使用量を計測前に必ず記録する。** 今回はこれが4GB埋まった状態での計測だった。
