# 作業ログ

## 2026-08-15 フェーズ0: ローカルAI環境の構築と要件・設計の確定

### 目的

ローカルで動くAI環境を作る。用途はチャット/文章作成とコーディング支援。

作業中に目的が2段階で明確化した。

1. 最終ゴールは **Claudeアプリのような自作デスクトップアプリ（他人にも配布できる単体アプリ）**
2. さらに **独自モデルそのものの開発**が目的に含まれる。
   RAGのような外付けの仕組みは代替にならない。ローカルで着手し、
   **一定水準に達したらクラウドへ移行する**前提

本フェーズはその土台および開発用バックエンドと位置づけた。

### やったこと

| 手順 | 内容 |
|---|---|
| 1 | ハードウェア確認: MacBook Air M3 / 16GB / ファンレス / 空き143GB |
| 2 | 開発機の環境台帳を確認し、既存プロジェクトのポート使用状況を把握 |
| 3 | `brew install ollama uv` → Ollama 0.32.13 / uv 0.12.5 |
| 4 | Open WebUI を **uv でネイティブ導入**（Python 3.11）。`~/.local/bin` を PATH へ |
| 5 | `qwen3:8b` / `qwen2.5-coder:7b` を取得（計9.2GB） |
| 6 | `modelfiles/` から `sophia-chat` / `sophia-coder` を作成 |
| 7 | `scripts/bench.py` でベースラインを計測 |
| 8 | 思考モードの影響を検証（下記 発見1） |
| 9 | 実行順を逆にした再計測で熱制限を確認（下記 発見2） |
| 10 | 要件定義書・設計書・ロードマップを作成 |
| 11 | 開発機の環境台帳を更新（ツール追加・ポート表新設・マシン仕様・プロジェクト節） |

### 検証で判明したこと

#### 発見1: 思考モードがトークン予算の9割を消費する

`qwen3:8b` は応答前に思考テキストを出力する。同一プロンプトでの比較:

| 条件 | 思考 | 本文 | 消費トークン |
|---|--:|--:|--:|
| 思考モード有効（既定） | 1,075文字 | 112文字 | 300（上限到達） |
| 思考モード無効 | 0文字 | 56文字 | 32 |

TTFT 15〜19秒の正体はこれ。生成上限が小さいと**本文に到達しないまま打ち切られる**
（ベンチのcodeプリセットで実際に発生し、TTFTがnanになった）。

→ 設計へ反映: 思考を折りたたみ領域に分けて表示し、無言の待機を作らない（FR-17/18、DESIGN 第6章）。

#### 発見2: ファンレスの熱制限で速度が約半分になる

実行順を入れ替えて再計測した結果:

| 実行順 | sophia-coder | sophia-chat |
|---|--:|--:|
| chat先行 | 9.0 tok/s（2番目） | 13.4 tok/s（1番目） |
| coder先行 | 14.1 tok/s（1番目） | 7.0 tok/s（2番目） |

**先に走った方が速い。** モデルの性能差ではない。`sophia-chat` のTTFTも
冷間15.4秒に対し高温時28.9秒へ悪化した。

→ 設計へ反映: 連続使用時の実力値を **7〜9 tok/s** とし、NFR-03b の基準とした。
当初 NFR-03 に置いた「10 tok/s以上」は連続負荷下では**未達**であるため、
冷間13 tok/s以上・連続7 tok/s以上へ改めた。

### 判断と理由

- **Open WebUI を Docker ではなく uv で入れた**
  16GB機で Docker Desktop を常駐させると VM だけで数GB消費し、モデルに回せるメモリが減るため。
  開発機の従来方針では MySQL 等は Docker を使うが、常駐コストが直接性能に効く本件では例外とした。

- **Open WebUI のポートを 8081 にした**
  既定の 8080 は同一マシン上の別プロジェクトが使用中だった。
  開発機のポート台帳を事前に確認して検知した。

- **常駐サービス（brew services）ではなくスクリプト起動にした**
  チューニングのたびに再起動が要るため、設定が `.env` に見える形で置かれ、
  明示的に再起動できる方が扱いやすい。

- **Flash Attention と KVキャッシュ量子化(q8_0)を既定で有効にした**
  長いコンテキストのメモリをほぼ半減できる一方、品質劣化は小さい。
  16GBという制約下では最もコスパが良い。Homebrew の caveats も同じ組み合わせを提示している。

- **`OLLAMA_MAX_LOADED_MODELS=1` にした**
  既定のままだとチャット用とコード用が同時にメモリに載り、スワップして両方遅くなるため。

- **ベースモデルを Qwen 系のままとし、国産モデルへは切り替えなかった**
  (1) 日本語モデルの多くは Llama/Qwen への追加学習で、土台の性能に依存する
  (2) コード特化の国産モデルがほぼ無く、用途の半分が弱くなる
  (3) **Qwen3-8B / Qwen2.5-Coder-7B は Apache 2.0 で再配布・商用利用が可能**であり、
  単体配布アプリという要件と噛み合う。
  なお「国産AI」という表現は、モデルが国産でない以上、事実と食い違うため採用しない。

- **推論エンジンを抽象化する方針を先に決めた**（DESIGN 第4章）
  開発は Ollama、配布は同梱ランタイムと実行方法が変わるため。
  後から差し替えると UI ごと作り直しになる。

- **文書を再構成した**
  当初の `APP_PLAN.md` を廃止し、
  `REQUIREMENTS.md`（要件定義）/ `DESIGN.md`（設計）/ `ROADMAP.md`（行程）へ分割。
  要件と設計と計画が1ファイルに混在していると、変更時にどこを直すべきか判断できないため。

### 検証結果

| 項目 | 結果 |
|---|---|
| `make help` / `make models` / `make status` | 動作確認済み |
| ベースライン計測 | 取得済み（`BENCH_RESULTS.md`） |
| 思考モードの影響 | API直叩きで確認。日本語の応答生成も併せて確認（発見1） |
| 熱制限の再現 | 実行順逆転で確認（発見2） |
| Open WebUI の起動 | 確認済み。`127.0.0.1:8081` で待ち受け（localhost限定） |
| Open WebUI → Ollama 接続 | 確認済み。モデルセレクタに `sophia-chat:latest` が表示される |
| Open WebUI 上での会話操作 | **未確認** ← 実ブラウザでの確認が残っている |
| `bench.py` / `serve.sh` の構文 | 確認済み |

### 作業中に修正した問題

| 問題 | 対処 |
|---|---|
| `open-webui` が PATH で見つからず起動失敗 | uv が `~/.zshenv` に PATH を追加するが、シェル設定の読み込み状況に依存する。`serve.sh` 側で `~/.local/bin` を明示的に PATH へ追加した |
| `WEBUI_AUTH=False` で全インターフェース待ち受け | ログイン不要のまま同一ネットワークから到達可能になるため、`--host 127.0.0.1` を明示して localhost 限定にした |
| ベンチを冷却なしで連続実行した（計測手順の不備） | `bench.py` に `--cooldown` を追加。複数モデルを冷却なしで測ると警告を出すようにした |
| Open WebUI が `.webui_secret_key` を生成 | `.gitignore` に追加 |

### 未実施・残課題

- **Open WebUI 上での会話操作が未確認**（推論自体はAPIで確認済み。実ブラウザで要確認）
- Electron アプリ本体は未着手（トラックA1以降）
- 品質面の評価基盤が未整備。速度は測れるが、出力の質を判定する手段がない。
  ROADMAP 第3章のとおり、**モデル開発（M1）着手前に作る必要がある**
- git 未初期化
- モデルのライセンス最終確認（REQUIREMENTS 未決事項#4）

---

## 次のステップ

ROADMAP 第5章の作業順に従う。

1. 品質評価セットの作成（自分の実作業から10〜20題）
2. モデルマージを1回通す（M1）
3. Electron骨格 + 思考モード表示（A1）

1と3は並行可能。着手前に Apple Developer アカウントの要否（未決事項#1）を
確認しておくとフェーズA4で詰まらない。
