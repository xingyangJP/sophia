# 作業ログ

## 2026-08-15 フェーズ0: ローカルAI環境の構築と要件・設計の確定

### 目的

ローカルで動くAI環境を作る。用途はチャット/文章作成とコーディング支援。

作業中に目的が2段階で明確化した。

1. 最終ゴールは **Claudeアプリのような自作デスクトップアプリ（他人にも配布できる単体アプリ）**
2. さらに **独自モデルそのものの開発**が目的に含まれる。
   RAGのような外付けの仕組みは代替にならない。ローカルで着手し、
   **一定水準に達したらクラウドへ移行する**前提

本フェーズはその土台および開発用バックエンドと位置づけた。

※ **2026-08-16 に最終ゴールは再定義された。** 現在の最終ゴールは
「現在の約1/1000のエネルギーで同じ仕事をするAI」（docs/VISION.md）であり、
アプリも独自モデルもその通過点である。本節は**当時の認識の記録**として残す。

### やったこと

| 手順 | 内容 |
|---|---|
| 1 | ハードウェア確認: MacBook Air M3 / 16GB / ファンレス / 空き143GB |
| 2 | 開発機の環境台帳を確認し、既存プロジェクトのポート使用状況を把握 |
| 3 | `brew install ollama uv` → Ollama 0.32.13 / uv 0.12.5 |
| 4 | Open WebUI を **uv でネイティブ導入**（Python 3.11）。`~/.local/bin` を PATH へ |
| 5 | `qwen3:8b` / `qwen2.5-coder:7b` を取得（計9.2GB） |
| 6 | `modelfiles/` から `sophia-chat` / `sophia-coder` を作成 |
| 7 | `scripts/bench.py` でベースラインを計測 |
| 8 | 思考モードの影響を検証（下記 発見1） |
| 9 | 実行順を逆にした再計測で熱制限を確認（下記 発見2） |
| 10 | 要件定義書・設計書・ロードマップを作成 |
| 11 | 開発機の環境台帳を更新（ツール追加・ポート表新設・マシン仕様・プロジェクト節） |

### 検証で判明したこと

#### 発見1: 思考モードがトークン予算の9割を消費する

`qwen3:8b` は応答前に思考テキストを出力する。同一プロンプトでの比較:

| 条件 | 思考 | 本文 | 消費トークン |
|---|--:|--:|--:|
| 思考モード有効（既定） | 1,075文字 | 112文字 | 300（上限到達） |
| 思考モード無効 | 0文字 | 56文字 | 32 |

TTFT 15〜19秒の正体はこれ。生成上限が小さいと**本文に到達しないまま打ち切られる**
（ベンチのcodeプリセットで実際に発生し、TTFTがnanになった）。

※ 上段は1,187文字を300トークンで出した計算になり、日本語の校正値（1文字≒0.5トークン）と
合わない。思考が英語だった可能性があるが記録が無く未確認（DESIGN 2.1 に未解決として記載）。

→ 設計へ反映: 思考を折りたたみ領域に分けて表示し、無言の待機を作らない（FR-17/18、DESIGN 第6章）。

#### 発見2: ファンレスの熱制限で速度が約半分になる

実行順を入れ替えて再計測した結果:

| 実行順 | sophia-coder | sophia-chat |
|---|--:|--:|
| chat先行 | 9.0 tok/s（2番目） | 13.4 tok/s（1番目） |
| coder先行 | 14.1 tok/s（1番目） | 7.0 tok/s（2番目） |

**先に走った方が速い。** モデルの性能差ではない。`sophia-chat` のTTFTも
冷間15.4秒に対し高温時28.9秒へ悪化した。

→ 設計へ反映: 連続使用時の実力値を **7〜9 tok/s** とし、NFR-03b の基準とした。
当初 NFR-03 に置いた「10 tok/s以上」は連続負荷下では**未達**であるため、
冷間13 tok/s以上・連続7 tok/s以上へ改めた。

### 判断と理由

- **Open WebUI を Docker ではなく uv で入れた**
  16GB機で Docker Desktop を常駐させると VM だけで数GB消費し、モデルに回せるメモリが減るため。
  開発機の従来方針では MySQL 等は Docker を使うが、常駐コストが直接性能に効く本件では例外とした。

- **Open WebUI のポートを 8081 にした**
  既定の 8080 は同一マシン上の別プロジェクトが使用中だった。
  開発機のポート台帳を事前に確認して検知した。

- **常駐サービス（brew services）ではなくスクリプト起動にした**
  チューニングのたびに再起動が要るため、設定が `.env` に見える形で置かれ、
  明示的に再起動できる方が扱いやすい。

- **Flash Attention と KVキャッシュ量子化(q8_0)を既定で有効にした**
  長いコンテキストのメモリをほぼ半減できる一方、品質劣化は小さい。
  16GBという制約下では最もコスパが良い。Homebrew の caveats も同じ組み合わせを提示している。
  → **この判断根拠は 2026-08-16 の実測で否定された**（削減は常駐比8%であって半減ではなく、
  速度も f16 の方が約6%速い）。結論として q8_0 は据え置いたが理由は別
  （ページング回避）。同日の「判断と理由」および TUNING 第5章を参照。

- **`OLLAMA_MAX_LOADED_MODELS=1` にした**
  既定のままだとチャット用とコード用が同時にメモリに載り、スワップして両方遅くなるため。

- **ベースモデルを Qwen 系のままとし、国産モデルへは切り替えなかった**
  (1) 日本語モデルの多くは Llama/Qwen への追加学習で、土台の性能に依存する
  (2) コード特化の国産モデルがほぼ無く、用途の半分が弱くなる
  (3) **Qwen3-8B / Qwen2.5-Coder-7B は Apache 2.0 で再配布・商用利用が可能**であり、
  単体配布アプリという要件と噛み合う。
  なお「国産AI」という表現は、モデルが国産でない以上、事実と食い違うため採用しない。

- **推論エンジンを抽象化する方針を先に決めた**（DESIGN 第4章）
  開発は Ollama、配布は同梱ランタイムと実行方法が変わるため。
  後から差し替えると UI ごと作り直しになる。

- **文書を再構成した**
  当初の `APP_PLAN.md` を廃止し、
  `REQUIREMENTS.md`（要件定義）/ `DESIGN.md`（設計）/ `ROADMAP.md`（行程）へ分割。
  要件と設計と計画が1ファイルに混在していると、変更時にどこを直すべきか判断できないため。

### 検証結果

| 項目 | 結果 |
|---|---|
| `make help` / `make models` / `make status` | 動作確認済み |
| ベースライン計測 | 取得済み（`BENCH_RESULTS.md`） |
| 思考モードの影響 | API直叩きで確認。日本語の応答生成も併せて確認（発見1） |
| 熱制限の再現 | 実行順逆転で確認（発見2） |
| Open WebUI の起動 | 確認済み。`127.0.0.1:8081` で待ち受け（localhost限定） |
| Open WebUI → Ollama 接続 | 確認済み。モデルセレクタに `sophia-chat:latest` が表示される |
| Open WebUI 上での会話操作 | 当時は**未確認**。→ 2026-08-16 の計測（発見4）で実ブラウザから送信して確認済み |
| `bench.py` / `serve.sh` の構文 | 確認済み |

### 作業中に修正した問題

| 問題 | 対処 |
|---|---|
| `open-webui` が PATH で見つからず起動失敗 | uv が `~/.zshenv` に PATH を追加するが、シェル設定の読み込み状況に依存する。`serve.sh` 側で `~/.local/bin` を明示的に PATH へ追加した |
| `WEBUI_AUTH=False` で全インターフェース待ち受け | ログイン不要のまま同一ネットワークから到達可能になるため、`--host 127.0.0.1` を明示して localhost 限定にした |
| ベンチを冷却なしで連続実行した（計測手順の不備） | `bench.py` に `--cooldown` を追加。複数モデルを冷却なしで測ると警告を出すようにした |
| Open WebUI が `.webui_secret_key` を生成 | `.gitignore` に追加 |

### 未実施・残課題

- ~~Open WebUI 上での会話操作が未確認~~ → **2026-08-16 の計測で解消**（発見4）
- 品質面の評価基盤が未整備。速度は測れるが、出力の質を判定する手段がない。
  ROADMAP 第3章のとおり、**モデル開発（M1）着手前に作る必要がある**（未解決のまま）
- ~~モデルのライセンス最終確認（REQUIREMENTS 未決事項#4）~~
  → **2026-08-17 に確定**（Qwen3-8B / mlx-community/Qwen3-8B-4bit とも apache-2.0。同日の節を参照）

---

## 2026-08-16 入力処理（プリフィル）が遅い問題の切り分け

### 目的

Open WebUI 経由の応答が遅い。入力処理速度がベンチでは 3,610 tok/s、
実環境では 140 tok/s と**25倍食い違う**。この差の正体を突き止める。

当初の主要な仮説は「`OLLAMA_KV_CACHE_TYPE=q8_0` が Metal で入力処理を遅くしている」だった。

### 結論（先に）

**仮説はすべて否定され、前提そのものが誤っていた。**
25倍の食い違いは実在せず、**3,610 tok/s の側が偽値**だった。
真の原因は設定ではなく、**Open WebUI が「こんにちは」の一言に4,786トークン送っていたこと**。

### 検証で判明したこと

#### 発見3: 「25倍の差」は存在しない。ベンチ側が偽値だった

`scripts/bench.py` は同一プロンプトをウォームアップ＋計測回で繰り返すため、
2回目以降はプレフィックスキャッシュにほぼ全命中する。
実計算はほぼゼロなのに `prompt_eval_count` は全トークンを数えるため、
割り算した tok/s だけが何十倍にも膨らんでいた（Ollama の既知の挙動）。

物理的にありえないことは算術で確認できる。
2 × 8.19e9 params × 3,610 tok/s = **59 TFLOP/s**。
M3（8コアGPU）の FP16 理論ピークは約6.6 TFLOP/s で、**9倍超過**している。

プロンプト先頭に nonce を入れてキャッシュ命中を壊して測り直した真値は **114〜167 tok/s**
（理論ピークの33〜41%で妥当）。**実環境で見えていた140 tok/s は異常値ではなく、素の実力値。**

→ 「遅い原因を探す」ではなく「そもそも遅くない。長すぎるだけ」という問題に変わった。

#### 発見4: 4,786トークンの95%は Open WebUI が自動注入していた

素の Ollama API へ「こんにちは」を送ると245トークン。
Open WebUI 0.11.0 経由だと4,786トークン。**差の約4,550トークンはクライアント由来。**

正体は native function calling 用の**ビルトインツール群のJSONスキーマ**であり、
システムプロンプトでもRAGでもなかった
（`utils/middleware.py:2643-2649` で `use_builtin_tools` が立ち、
`utils/payload.py:321-322` で Ollama のペイロードへコピーされる）。
なおツールの個数は当初32個と数えたが、カテゴリ別の内訳を足すと35個になり整合していない。
内訳のトークン数も合計が実測を約456トークン超過している（TUNING 第2章の注記を参照）。

→ 入力処理は正常に約140 tok/s で動いており、**4,786トークンで32〜35秒かかるのが正しい挙動**。

#### 発見5: `.env` に書いた設定の半分は「効かない」か「見当違い」だった

- **効かない**: Open WebUI は設定をDBに保存し、環境変数は
  「DBにまだ行が無いキー」にしか適用されない（`models/config.py:256`）。
  初回起動後に `.env` を書き換えても黙って無視される。
  `.env` 冒頭の「編集して `make restart` すれば反映される」が**成立していなかった**。
- **見当違い**: 既存の `ENABLE_*_GENERATION` 群は裏のLLM呼び出し回数を減らすが、
  **1回あたりのプロンプト長は1トークンも減らさない**。
  `ENABLE_WEB_SEARCH=False` に至っては既定が既に False で削減量ゼロ。
  「既定で有効だとプロンプトにツール定義が注入される」というコメントは誤りだった。

#### 発見6: 「512トークンごとにKVを捨てて再計算」も誤読だった

`cached n_tokens` は512ずつ**増えて**おり、チャンク処理の記帳にすぎない。
決定的な証拠は実計算トークン数で、**4,560 = 4,793（全体） − 233（キャッシュ命中）** が
ぴったり一致する。捨てて再計算していればこの数は数倍に膨らむ。

#### 発見7: この機体ではメモリ逼迫が熱より大きい雑音源

計測中の空きメモリ1.3〜2.8GB、スワップ使用6〜7GB/8GB。
**同一条件・4,786トークン16件で31.7〜154.5秒（4.9倍の開き）。**
1回の計測中にページアウト90,104ページ（約1.4GB）が起きたブロックは、全長で速度が半分になった。

**条件間の差（KV型で6%）より同一条件内のばらつきの方が大きい。**
この状態では設定のA/Bをやっても判定できない。実際 `num_batch` 512 vs 1024 は判定不能に終わった。

### 判断と理由

- **`OLLAMA_KV_CACHE_TYPE` は `q8_0` のまま据え置いた**
  計測は f16 が約6%速いことを示した（3ラウンドとも f16 が速い）が、
  常駐メモリは f16 6.5〜6.6GB / q8_0 6.0〜6.1GB で**0.5GBの差**がある。
  スワップ6〜7GB使用の逼迫下ではページングが最大4.9倍の劣化を生んでおり、
  **6%の速度差より0.5GBの確保を優先**した。
  ただし「速度で有利だから q8_0」ではないので、TUNING.md の
  「効果：大 / コスパ最良」「メモリをほぼ半減」という記述は誤りとして全面的に書き換えた
  （実際の削減は常駐比8%であって半減ではない）。
  メモリに余裕がある使い方をするなら f16 の方が速い、と併記した。

- **`ENABLE_PERSISTENT_CONFIG=False` を追加した**
  これを入れないと `.env` の Open WebUI 側設定が今後一切反映されない（発見5）。
  「`.env` を編集して `make restart` すれば反映される」という本プロジェクトの前提を
  実際に成立させるための措置。副作用として管理画面での設定変更が再起動で消えるが、
  1人利用であり、設定の権威が2箇所にある方が事故を生むため許容した。

- **`ENABLE_MEMORIES` / `ENABLE_CALENDAR` / `ENABLE_AUTOMATIONS` / `ENABLE_NOTES` を追加した**
  ツール定義そのものを減らす唯一の環境変数群で、合計約3,080トークン削減。
  逆に既存の9個は1つも削除しなかった。**全て実在する正しい名前だった**ため
  （インストール済みパッケージの `config.py` で17個すべて確認済み）。
  ただし効果を誤解させるコメントは全て書き直した。

- **環境変数だけでは足りないことを明記した**
  knowledge / time / chats / tasks の約1,700トークンには対応する環境変数が存在せず
  （`ENABLE_KNOWLEDGE` などは実在しない）、モデルの capability
  「Builtin Tools」を off にするしかない。
  **環境変数だけの到達点は約1,600トークン＝プリフィル約10秒**で、目標にぎりぎり届かない。

- **`bench.py` の入力処理列を「無効」として BENCH_RESULTS.md に明記した**
  数値を消すのではなく、無効である旨と理由を残した。
  過去の記録を削ると「なぜ3,610という数字が出回っていたか」が追えなくなるため。

- **2026-08-16 13:00 の計測節に「⚠ 無効」を付けた**
  `num_batch` を固定しておらず q8_0=512 / f16=1024 で走っていたため、
  KV型の比較として成立していない。自分の計測でも同じ罠を踏みうるので、
  削除せず失敗例として残した。

### 変更したファイル

| ファイル | 内容 |
|---|---|
| `.env` | KV型の判断根拠を実測値で書き直し。`ENABLE_PERSISTENT_CONFIG=False` と ツール定義を減らす4変数を追加。誤ったコメントを全て修正 |
| `docs/TUNING.md` | 第1章「入力プロンプトの長さ」・第2章「クライアントが注入するプロンプト」・第7章「num_batch」を新設。第5章 KVキャッシュ量子化を全面書き換え。測り方の作法に偽値の警告と計測プロトコルを追加。以降の章を繰り下げ |
| `docs/BENCH_RESULTS.md` | 長尺計測（273〜7,688トークン）と総括を追記。偽値・無効節の警告を追加 |
| `docs/DESIGN.md` | 第2.0.2章（入力処理の実測）・2.2章（プロンプト肥大を持ち込まない）・2.3章（10秒目標の達成条件）・2.4章（NFR-03 の問題）を追加。リスク8・9を追加 |
| `docs/MODELS.md` | TUNING.md の章番号繰り下げに追従 |
| `scripts/bench-prompt.py` | 新規。入力処理専用の計測スクリプト |

### 検証結果

| 項目 | 結果 |
|---|---|
| KV型 q8_0 vs f16 の A/B | 実施済み。f16 が約6%速い（中央値 32.5s vs 34.5s、3ラウンド） |
| 「512刻みで再計算」の検証 | 否定。実計算 4,560 = 4,793 − 233 の一致で確定 |
| 3,610 tok/s の正体 | 偽値と確定（59 TFLOP/s は理論ピークの9倍） |
| プロンプト長と速度の関係 | 実測済み（273 / 1,022 / 4,792 / 7,688 トークンの4点） |
| Open WebUI の4,786トークンの内訳 | ソース読解と実トークナイザによる再現計算で特定（実測4,786 vs 計算4,887、誤差2.1%） |
| `num_batch` 512 vs 1024 | **判定不能。** ばらつきが条件差より大きい |
| `.env` の構文 | `bash -c 'set -a; source .env'` で確認済み。全値が期待通り |
| **追加した環境変数の実際の効果** | **未検証** ← Open WebUI を起動して `task.n_tokens` の確認が必要 |

### 未実施・残課題

- **`.env` の変更が実際にプロンプト長を減らすか未検証。**
  Open WebUI を起動し `logs/ollama.log` の `task.n_tokens` が
  4,786 → 約1,600 になることを確認する。**これをやるまで対策は完了していない**
- モデル capability「Builtin Tools」を off にする操作は未実施（GUI操作が必要）。
  約231トークンまで落ちる見込みだが未実測
- `scripts/bench.py` 側の入力処理計測は未修正。
  nonce 挿入と理論ピーク超過時の警告を入れるべき（今は `bench-prompt.py` が別にあるだけ）
- **NFR-03「1秒以内に何かが表示される」の達成条件が定義できていない。**
  プリフィルは273トークンでも1.6秒かかり、`sophia-chat` は SYSTEM だけで223トークン使う。
  入力が約170トークン以内でないと満たせない。要件の見直しが必要（DESIGN 第2.4章）
- `num_batch` の判定は、メモリを空けたクリーン状態で測り直す必要がある
- コンテキストシフト（`num_ctx=8192` 超過）の崖が実際に起きるか未確認
- 連続実行での1.55倍の劣化について、熱とページングのどちらが主因か切り分けていない

---

## 2026-08-16 Electron から SwiftUI + MLX Swift への転換と、フェーズA1の骨格

### 目的

同じ日に2つのことをやった。

1. 最終ゴールを言語化して固定する（docs/VISION.md、commit 99cdbf0）
2. その目標から見て土台を選び直し、アプリ本体の骨格を作る（commit e44a567）

### やったこと

| 手順 | 内容 |
|---|---|
| 1 | 最終ゴールを **「現在の約1/1000のエネルギーで同じ仕事をするAI」**と定義し `docs/VISION.md`（389行）に記録。アプリも独自モデルもその通過点と位置づけた |
| 2 | MLX Swift で A1 が実現できるかを**実装前に**調べ、`docs/MLX_SWIFT.md`（1,054行）にまとめた |
| 3 | Electron 前提を捨て、SwiftUI + MLX Swift でアプリの骨格を実装（74ファイル / 10,344行追加。うち `Sophia/Sources/` 配下の Swift が6,656行） |
| 4 | 依存を Xcode の Package Dependencies で固定: mlx-swift 0.31.6 / mlx-swift-lm（revision `d7dc03d`）/ GRDB 7.11.1 |
| 5 | Makefile に `app` / `app-release` / `app-run` / `app-test` / `app-clean` / `app-setup` / `test-inference` を追加 |
| 6 | 会話の永続化（GRDB）を、スキーマだけでなくアプリ本体からの逐次書き込みまで結線した |

### 判断と理由

- **Electron をやめて SwiftUI + MLX Swift にした**
  VISION の第3因子（層ごとに計測し、要らない計算を早期に打ち切る）は
  **推論の中身に手が届くこと**が前提になる。Electron + Ollama では推論はサーバの内側にあり、
  層ごとの時間も KV の扱いも触れない。MLX Swift なら生成ループの中に自前の部品を差し込める
  （その差し込み口の確保が下の `generate` の選択理由でもある）。
  **なお捨てたのは実装ではなく計画である。** 2026-08-15 の残課題に
  「Electron アプリ本体は未着手」とあるとおり、Electron 版のアプリ本体は1行も書いていない。
  失ったのは Electron 前提で書いた調査（`docs/UI_NATIVE.md` など）の一部有効性だけで、
  転換コストは低かった。**この判断が半日で済んだのは、着手前に方向を疑ったからである。**

- **アプリのモデルを `mlx-community/Qwen3-8B-4bit`（MLX形式）にした**
  Ollama 用の GGUF は MLX では読めない。したがってアプリと開発用スタック（Ollama）は
  **モデルの実体からして別系統**になる。この事実が後日いくつもの取り違えを生む（2026-08-17 発見9）。

- **mlx-swift-lm を revision 固定で使った**
  思考テキストの分離に使う API がリリース版（3.31.4）に無く、`main` にしか無いため。
  版を動かすと分離が壊れるので、`project.pbxproj` で `d7dc03d` に固定した。

- **`ModelContainer.generate` ではなく `perform` + 自由関数 `generate` を使った**
  **`components:` を渡せるのがそちらだけだから。** VISION 第3因子の差し込み口を、
  まだ使わないうちから通しておくための選択。MLX_SWIFT.md の当初案（第12.2節）は
  `ModelContainer.generate` を指していたが、実装で改めた。

- **思考の分離は公式の `ReasoningEventEmitter` を被せる形にし、自作の `ThinkingSplitter` は
  フォールバックに置いた**
  モデル側が分離情報を持っているならそれに従うのが正しい。自作側は
  分離情報を持たないモデルへの備えとして残した（この判断の帰結は 2026-08-17 発見11）。

### 検証結果

| 項目 | 結果 |
|---|---|
| ビルド | 成功（DerivedData のビルドログに `Build succeeded`。成果物 `Sophia.app` は全ソースより新しい） |
| 起動して操作したか | **記録が無い。** この日は「ビルドが通った」以上のことは確かめていない |
| 永続化層（DB）の単体テスト | green |
| MLX 経路の速度（TTFT / tok/s） | **未計測** |

### 未実施・残課題

- **A1 完成条件9項目の実機検証が未実施**（翌日にコードで裏取りした。2026-08-17 発見12）
- MLX 経路の実測が1つも無い。`docs/DESIGN.md` 2.6章は「未計測」のまま
- Electron 前提で書かれた文書が残っている（`docs/UI_NATIVE.md`、本文書自身を含む）
- アプリ本体のバイナリサイズ（NFR-06: 300MB以内）が未計測

---

## 2026-08-17 A1の結合 — 日本語入力の修正、自己認識、計測経路の確保

### 目的

**実機で完成条件を確かめる前に、確かめられなくしている穴を塞ぐ。**
この日の作業は3つとも「機能を足す」話ではなく、
**日本語が打てない / 名前を名乗れない / 測った結果が残らない** という
検証そのものを妨げる欠陥への対処である。

### やったこと

| 手順 | 内容 |
|---|---|
| 1 | 入力欄で日本語が打てない不具合を修正（commit c4e28a9） |
| 2 | アプリ側にモデルの自己認識（**FR-23**）を実装（commit 8f099e3）。※ 実装時のコメントは誤って FR-21 と書いており、同日中に修正した |
| 3 | Makefile に `app-stats` / `stats-tail` を追加し、`[STATS]` 行の逃げ場を作った（同 8f099e3） |
| 4 | モデルのライセンスを確定（REQUIREMENTS 未決事項#4 への回答） |
| 5 | A1 完成条件9項目を実コードで裏取りし、実機で何をどう見るかとリスクを洗い出した |
| 6 | 文書全体の棚卸し（方向転換前の記述・Electron 前提・実態との乖離の洗い出し） |

### 検証で判明したこと

#### 発見8: IME変換中に SwiftUI 側が入力欄を空で上書きしていた

**症状は「日本語だけが打てない。英数字は打てる」。** 原因の連鎖はこうだった。

1. `ComposerTextView.updateNSView` が無条件に `textView.string` へ代入していた
2. **変換中（未確定文字がある間）は `textDidChange` が発火しない。**
   したがって SwiftUI 側の `model.input` は**空のまま**
3. 一方 `textViewDidChangeSelection` は**プレースホルダの表示判定のために発火する**
4. その再描画が `updateNSView` を呼び、「AppKit側は "にほん"、SwiftUI側は空」を見て
   **空で上書きする**。未確定文字（マークドテキスト）ごと消える
5. 英数字は `textDidChange` が走って SwiftUI 側にも文字が届くので無事

→ `hasMarkedText()` が真のときは代入しないガードを入れた。

**テストで固定した方法。** `NSViewRepresentableContext` は自前で組み立てられないため、
`makeNSView` のままだと入力欄の実物を単体テストから触れない。
`Context` を取らない入口（`makeScrollView` / `apply(to:coordinator:)`）を分けて、
実物の `NSTextView` に対して10件を検証した。
中でも**ローマ字の全打鍵（k → こ → こん → … → こんにちは）を通しで再現する**ものが本体で、
「1打鍵ずつ見れば正しいが、通すと消える」というこの種のバグを掴まえる形になっている。
**ガードを外すと `testMarkedTextSurvivesSwiftUIUpdate` が
`("") is not equal to ("にほん")` で落ちることも確認済み**
（テストが本当にこのバグを見張っているかの確認）。

#### 発見9: `make models` を実行してもアプリには1文字も届かない

アプリが読むのは `mlx-community/Qwen3-8B-4bit`（MLX形式）で、
`make models` が焼き直すのは Ollama の `sophia-chat`（GGUF）。**完全に別系統である。**
`modelfiles/sophia-chat.Modelfile` の `SYSTEM` はアプリには**絶対に届かない**。

これは単なる豆知識ではない。`docs/TUNING.md` 第10章が
「システムプロンプトは `modelfiles/` で管理し、育てたら `make models` で焼き直す」と
**アプリに対しては誤ったことを書いていた**（同日の文書見直しで修正）。
アプリ側の system は `Sophia/Sources/Shared/ChatOptions.swift` の `SophiaDefaults.systemPrompt` にあり、
**Ollama 側とアプリ側の文言は手で合わせるしかない。ここが唯一の食い違いリスク点**である。

#### 発見10: `make app-run` では `[STATS]` 行が1バイトも残らない

`make app-run` は `open Sophia.app`。**LaunchServices 経由で起動したプロセスは
fd 0/1/2 がすべて `/dev/null` に繋がる**（稼働中の GUI プロセスを `lsof -p <pid> -a -d 0,1,2` で
実測して確認した）。計測ログの出力は生の `write(2)` なので **`log stream` でも拾えない。**

つまり「`SOPHIA_LOG_STATS=1` を付けたのにログが出ない」を
**「計測コードが動いていない」と誤診する構造**になっていた。
対処は `open --stderr <path>` で fd 2 を付け替えること
（使い捨ての .app で実測し、追記（O_APPEND）であること・`--env` が launchd 経由でも
プロセスに届くことも確認済み）。これを `make app-stats` として実装した。

#### 発見11: 本番で走る思考分離は `ThinkingSplitter` ではない

Qwen3 は必ず `reasoningConfig` を持つ（`Qwen3.swift` → `LLMModelFactory` → `MLXEngine`）。
`MLXEngine.makeSeparator` は分離情報があれば公式側を選ぶので、
**`mlx-community/Qwen3-8B-4bit` では自作の `ThinkingSplitter` は1行も実行されない。**
実際に走るのは公式 `ReasoningEventEmitter` を被せた `ReasoningEmitterSeparator` である。

したがって **`scripts/test-thinking-splitter.swift` の28件は「本番では走らない側」を
検証している。** テストの数が多いことが FR-17 の安心材料にはならない。
本番側を見ているのは `scripts/test-splitter-vs-official.swift` だが、
こちらは公式実装のソースを取り込んで文字列比較しているだけで、
**実モデルの出力に対しては未検証。**
`ThinkingSplitter` はフォールバック経路として意図して残しているものなので削らないが、
**「本番では走らない」と明記しないと、次の担当者が守られていると誤解する。**

#### 発見12: A1完成条件9項目のうち、コードだけで判定できるのは2項目

9項目を実コードで裏取りした結果:

| 区分 | 項目数 |
|---|--:|
| 実装が存在しない | **0**（全項目に実装がある） |
| コードだけで「満たしている」と言える | **2**（コードハイライト、バージョン表示） |
| 実機で動かさないと判定できない | **7** |
| 実行時検証の記録がある | **0** |

**「実装した」と「動くことを確かめた」の間に7項目ぶんの距離がある。**
`docs/MLX_SWIFT.md` 第0章の表は列見出しが「判定」で全て「可」なので、
実装前の技術調査であることを知らないと**9項目すべて達成済みに見える**（同日の見直しで注記した）。

#### 発見13: 計測値のうち、どれが実測でどれが概算か

MLX が実値で埋めるのは6つだけ:
入力トークン / 出力トークン / プリフィル秒 / プリフィル tok/s / デコード秒 / ピークメモリ。

- **TTFT（初出力まで）と TTFR（本文まで）はアプリの壁時計**であって MLX の値ではない
- **思考トークン数は常に概算**（思考テキストの文字数 × 0.5）。MLX は思考だけの
  トークン数を返さないため、`[STATS]` に出る `think_tok` は実測ではない
- **中断した回**はプリフィル系の値が一切来ないので、**入力処理の計測材料に混ぜてはいけない**
- ピークメモリは過少報告の報告があり、単独では信用できない

「思考がトークン予算の9割」（発見1）の継続監視をこの概算値でやると、**偽の精度を持ち込む。**

> **その概算が実際に壊れていることが、同日の実測で確定した。**
> 1往復目は `think_tok=296` に対し `out=166` ─ **出力全体より思考が多いことは原理的にありえない。**
> 思考が英語のとき「0.5 tok/文字」の係数が約2倍過大に出る（英語は1文字≒0.25トークン前後）。
> **発見1の「思考が予算の9割」も、同じ係数で出した数字なら過大の疑いがある。**
> 対処は文字数からの概算をやめてトークナイザで数えること。
> **直せないなら概算値を出すのをやめる。壊れた数字を出し続けるより無いほうが良い。**

#### 発見14: モデルは apache-2.0。ただし配布物にライセンス文は入っていない

`Qwen/Qwen3-8B` と `mlx-community/Qwen3-8B-4bit` は**どちらも apache-2.0**
（HuggingFace のモデルカードで確認。変換は mlx-lm 0.24.0）。REQUIREMENTS 未決事項#4 はこれで閉じる。

**ただしアプリが実際に落としてくるのは重みとトークナイザだけである。**
スナップショットの中身を実地で数えたところ9ファイル
（`model.safetensors` / `model.safetensors.index.json` / `config.json` / `tokenizer.json` /
`tokenizer_config.json` / `special_tokens_map.json` / `added_tokens.json` / `merges.txt` / `vocab.json`）で、
**LICENSE も README も含まれない。**
A4 で同梱配布するなら、ライセンス文と帰属表示は**意図的に自分で入れる必要がある。**

#### 発見15: `@State` を含む Swift コードでアプリが確実に固まる

**シンタックスハイライタ（`SyntaxHighlighter`）の識別子ブランチが無限ループしていた。**

`isIdentifierStart` は `#` と `@` を識別子の先頭として受け付けるのに、
`isIdentifierPart` はどちらも含まない。結果、識別子として走査を始めたのに
`while` が**1文字も進まないまま `i = end`（= `i`）で戻り、同じ位置を永久に読み続ける。**

**`@` か `#` が来た時点で走査器が停止する。**
つまり **`@State` や `@MainActor` を含む Swift コードを1つでも表示すると必ず固まる。**
SwiftUI のコードを生成させたら確実に踏む経路だった。
Python やシェルが無事だったのは、`#` が行コメントとして**先に**処理されていたからにすぎない。

ハイライタは MainActor で走るため、**症状は「遅い」ではなく画面全体のフリーズ**（NFR-02 違反）。

修正は「先頭の1文字を必ず取り込む」＝ `end = i + 1` から始めるだけ。これで前進が保証される。
`SyntaxHighlighterTests` 7件を追加した。**ハングしたときにテストごと止まらないよう、
時間切れで落とす形にしてある**（無限ループの回帰は「失敗」ではなく「終わらない」形で現れるため）。

> **教訓: 文字集合を2つの述語に分けたら、両者の非対称が即ループになる。**
> 「開始として認めた文字が、継続としても認められるか」を必ず確認すること。

#### 発見16: 2往復目でプリフィルだけが9.2倍崩れる（原因未特定）

**アプリ初計測（2往復）で、2往復目の TTFT が 0.97秒 → 11.87秒 になった。**

| 項目 | 1往復目 | 2往復目 |
|---|--:|--:|
| 入力トークン | 104 | 156 |
| **プリフィル** | **123.26 tok/s** | **13.39 tok/s** |
| **TTFT** | **0.97秒** | **11.87秒** |
| デコード | 19.46 tok/s | **19.21 tok/s** |
| ピークメモリ | 4,615.61 MB | 4,656.19 MB |

**デコードは無傷**（−1.3%）。熱制限なら**デコードも道連れになるはず**で、そうなっていない。
入力は +50% しか増えていないのにプリフィル時間は13.9倍。

> **当初「ピークメモリも+40MBしか動いていない」を根拠に挙げたが、これは撤回した。**
> `peak_mb` は生成ごとにリセットされる**ピーク*アロケーション*量**であり
> （`MLXEngine.swift:290`）、**そのページが物理RAMにあるかスワップにあるかを知らない。**
> KVの膨張は否定できるが、**ページアウトは否定できない。**

**有力仮説: 待機中に重み（4.6GB）がページアウトされ、プリフィルが読み戻しの代金を払った。**
コード調査で前提条件はすべて確認した ─ **重みは wired されていない**（`wiredMemoryTicket` 既定 nil）/
モデルは往復をまたいで保持されている（**都度ロードではない**）/ 合間に `clearCache()` は呼ばれない /
メモリ圧への反応が0件 / `cacheLimit` 20MB で1往復ごとにバッファがOSへ返る。
そして **`prefill_s` の計測窓が「待機後に4.6GB全体へ最初に触る一回」と一致する。**
起動時点でスワップは 5,120M 中 4,056M が埋まっていた。

**消えた対抗仮説:** サーマル / チャンク境界差（既定ステップ512未満で同一経路）/
入力長そのもの / **モデルの都度ロード**。

**【未確認】** 残る候補は ①待機中のページアウト（本命）②KV持ち越し無しの再プリフィル
（**単独では9.2倍を説明しない**。156トークンを1往復目のレートで再プリフィルしても1.27秒）
③カーネル遅延コンパイル ④Debug増幅。**どれも裏を取っていない。**
切り分け手順は BENCH_RESULTS.md 2026-08-17 の節。

> **直す前に、静かな状態で再現するかを確かめること。**
> 計測はスワップ4GB使用の機体で並行作業の最中に取られており、
> **アプリの性質ではなく「そのときの機体の状態」を測った可能性が相当ある。**

**NFR-03（1秒以内に何かが出る）は2往復目で破れている。**
「初回だけ条件を満たす」のは満たしていないのと同じで、**体感を支配するのはこちら。**
**原因が①なら設計（DESIGN 第5章）に跳ね返る**ため、A1 の他の残作業より先に潰す。

> **1点しか見ていなかったら、この問題を見落としていた。**
> 引き継ぎの時点では `[STATS]` の1行目だけを拾っており、
> 「初計測で 19.46 tok/s、Ollama より速い」という**良い話で終わっていた。**
> **ログは最後の行まで読むこと。**

### 判断と理由

- **自己認識は Modelfile の SYSTEM 全文ではなく、冒頭3行だけを持ち込んだ**
  全文は概算 +219トークン／毎ターンで、入力予算1,000（`SophiaDefaults.inputTokenBudget`）の
  22%を**利用者が1文字も打つ前から**食う。
  「書き方の原則」「やりとりの原則」は**自己認識ではなく出力スタイルの調整**であり、
  モデルは指示が無くても大きくは外さない一方でコストは自己認識の1.8倍。
  役割の切替は A2 の `ProfileRecord`（FR-05）が本来の置き場なのでそちらへ回した。
  自己認識3行だけなら概算 +71トークン。
  **いずれも概算であって実測ではない**（下の残課題を参照）。

- **差し込み口を `ChatViewModel.engineMessages()` の1か所にした**
  この関数は `send()` と `estimatedInputTokens` の**共通の出所**である。
  ここに入れれば「画面に出るトークン数」と「実際に送るトークン数」が構造的に一致する。
  `send()` 側だけに足すと入力欄の予算警告が実送信より少ない嘘になり、
  **VISION の測定原則（無駄が痛みとして見えないと誰も減らさない）を最初に破るのがこのパターン。**
  エンジン側（`MLXEngine`）に注入させるのも退けた。Stub/Mock/将来の別実装でも
  同じ注入を再実装することになり、必ず食い違うため。

- **`SOPHIA_SYSTEM_PROMPT=0` で切れるようにした**
  理由は3つ。(a) NFR-03 の達成条件（入力が約170トークン以内）は、system 常時ONだと
  原理的に検証できなくなる。(b) VISION の適応度関数（品質 ÷ 消費エネルギー）を評価するには
  同じ問いを system あり/なしで走らせる必要があり、**切れないと比較実験そのものができない。**
  (c) BENCH_RESULTS を Ollama 側と並べるとき、片方だけ system を持っていると比較が壊れる。

- **`make app-stats` に `-n` を付けた**
  既に起動していると `open` は既存プロセスを前面に出すだけで、
  **新しい条件で起動し直してくれない。** この挙動で修正前のバイナリを測る事故を実際に1度起こした。

- **`app-stats` をビルドに依存させなかった**
  DerivedData を共有しているため、計測のたびにビルドが走ると熱とメモリの条件が揃わない。
  ビルド済みを前提とし、ビルドは別の手順に分けた。

- **テストのために `ComposerTextView` の構造を変えた**
  `NSViewRepresentableContext` は自前で組み立てられない以上、
  `Context` を取らない入口を分けるほかに入力欄をテストする手が無かった。
  「テストのために本体を変える」は避けたいが、
  **今回の不具合は実機で手で打ってみるまで誰も気づけなかった種類のもの**で、
  再発を人手の確認に委ねる方が高くつくと判断した。

- **実測値はまだ1つも書かない**
  数値の置き場（`docs/BENCH_RESULTS.md` の新しい節）と手順は決めたが、
  **親セッションのビルドと競合しない時間に実機で取るまでは表に何も入れない。**
  16GB機では Ollama とアプリを同時に動かせないので、Ollama 側との対照は
  同一条件では取れないことも先に分かっている。

### 変更したファイル

| ファイル | 内容 |
|---|---|
| `Sophia/Sources/UI/Chat/ComposerTextView.swift` | IME変換中は `textView.string` を上書きしないガード。`Context` を取らない入口を分離してテスト可能にした |
| `Sophia/Tests/ComposerTextViewTests.swift` | 新規。10件（ローマ字全打鍵の通し再現、変換1文字目でのプレースホルダ消去、変換中の Enter は確定で送信しない、送信後のクリア） |
| `Sophia/Sources/Shared/ChatOptions.swift` | `SophiaDefaults` に `systemPrompt` ほかを追加。**出所が Modelfile であること、`make models` では同期されないことをコメントに明記** |
| `Sophia/Sources/UI/Chat/ChatViewModel.swift` | `engineMessages()` の先頭に system を差し込み。ON/OFF のトグル |
| `Sophia/Tests/SystemPromptTests.swift` | 新規。4件（先頭に1件だけ届く / OFF で消える / トグルで見積りがその場で動く / Modelfile と文言が食い違わない） |
| `Makefile` | `app-stats` / `stats-tail` / `STATS_LOG` を追加。**なぜ `--stderr` が要るのか**をコメントに残した |

文書側（README / ROADMAP / DESIGN / REQUIREMENTS / VISION / MODELS / TUNING / MLX_SWIFT / UI 系）は
同日の一斉見直しで別途更新した。本文書もその対象である。

### 検証結果

| 項目 | 結果 |
|---|---|
| Xcode のテストターゲット | **全80件 green** |
| 日本語入力（実機） | **回復をユーザーが確認済み** |
| ガードが本当に効いているか | ガードを外すと該当テストが落ちることを確認済み |
| system が会話の先頭に1件だけ届くこと | 記録用エンジンを挟んだ単体テストで確認 |
| `[STATS]` が `open` では残らないこと | `lsof` で fd を実測して確認 |
| `open --stderr` で付け替わること | 使い捨ての .app で実測。追記であることも確認 |
| モデルのライセンス | apache-2.0（HuggingFace のモデルカード） |
| 配布物にライセンス文が入るか | **入らない。** snapshot の9ファイルを実地で確認 |
| **アプリが「Sophia」と名乗るか** | **実機で「ソフィア」と名乗ることをユーザーが確認済み** |
| コードブロックでのフリーズ | **修正済み。** `@`／`#` で走査器が無限ループしていた（発見15） |
| **system の実トークン増分** | **未計測。** 概算 +71 のみ |
| **MLX 経路の速度（TTFT / tok/s / プリフィル）** | **連続2往復のみ取得**（Debug / 思考ON）。基準値ではない。**2往復目で TTFT が0.97→11.87秒に崩れた**（発見16） |
| **A1 完成条件9項目の実行時検証** | **未実施**（9項目とも記録なし） |

### 未実施・残課題

- **A1 完成条件9項目の実機検証。** 7項目は実機でしか判定できない（発見12）
- **【最優先】2往復目のプリフィル崩れの切り分け**（発見16）。**A1 の他の残作業より先。**
  3往復以上を1セッションで回し、往復ごとに `vm.swapusage` を併記する。
  仮説①KV持ち越し無しの再プリフィル ②ページング ③カーネル遅延コンパイル ④Debug増幅
  ─ 切り分け方は BENCH_RESULTS.md 2026-08-17 の節。**①なら設計（DESIGN 第5章）に跳ね返る**
- **MLX 経路の実測が2往復ぶんしかない。** Debug ビルド（デバッガは非接続）・思考ON で
  `1往復目: in=104 ttft=0.97s prefill=123.26tps decode=19.46tps peak=4615.61MB`
  `2往復目: in=156 ttft=11.87s prefill=13.39tps decode=19.21tps peak=4656.19MB`。
  **Release ビルド・デバッガ非接続で取り直し**、冷間/連続の別と複数回の平均を揃えて
  BENCH_RESULTS.md へ Ollama と混ざらない形で追記する。比較先は
  2026-08-16 16:05 の真値であって、無効と確定した 3,610 tok/s ではない
- **思考トークンの概算が壊れている**（発見13）。`think_tok=296` > `out=166` は原理的にありえず、
  思考が英語のとき 0.5 tok/文字 が約2倍過大に出る。**トークナイザで数えるか、出すのをやめる**
- **Qwen3 のテンプレートが既定の system 文を挿入しているか未確認。**
  挿入しているなら自前の system は「純増」ではなく「置換」で、実コストは見積りより小さい。
  逆に、その既定が『私はQwen』と名乗らせている犯人である可能性もある。
  空メッセージで1回プロンプトを吐かせれば判明する
- **概算式（日本語 1文字 ≒ 0.5トークン）がこの用途に合っているか未確認。**
  Ollama 側の実績では指示文の実比は 0.756 で、**概算は約1.5倍甘い方向にずれる。**
  画面の「入力が約Nトークン」が予算に対して楽観的に出ている可能性がある
- **system 行を DB に書いていない。** A1 は履歴復元をしないので実害ゼロだが、
  A2 で復元を作ると「復元した会話に system が無い／二重に付く」が出る
- `ThinkingSplitter` は本番で走らない（発見11）。28件のテストが守っているのは
  フォールバック経路であり、本番側は実モデル出力に対して未検証
- シンタックスハイライタ（FR-06）と Markdown の単体テストが、**棚卸しの時点で1件も無かった。**
  **ハイライタ側は閉じた**（`SyntaxHighlighterTests` 7件。ハングを時間切れで落とす形の
  回帰テストを含む）。**Markdown 側は未整備のまま**
- **アプリ本体のバイナリサイズ（NFR-06: 300MB以内）が未計測。**
  GRDB を足したぶん依存が1つ増えており、悪化しているはずだが数字が無い
- Open WebUI 側の `.env` 検証（2026-08-16 の残課題）は**開発用バックエンドの課題として残る。**
  ただし16GB機ではアプリと Ollama を同時に動かせないので、アプリの実測と同時にはやらない

---

## 次のステップ

**アプリ（トラックA）を先に進める。順番は「実機で確かめる → 測る → 記録する」。**

### 1. A1 完成条件9項目の実機検証

コードの裏取りは済んでいる（発見12）。残っているのは**実際に起動して触ること**だけで、
7項目はそれ以外に判定手段が無い。見るべき要点:

- 起動して、サイドバー（エンジン名・版番号）と入力欄が出ること
- 日本語が伸びながら出て、文字化けが一瞬も出ないこと（FR-01）
- **プリフィル中**と**思考が流れ始めた後**の**2回**、中断できること（FR-02。
  この2つはコード上まったく別経路なので、片方だけ試すと穴が残る）
- 思考パネルが自動で畳まれ、**本文側に `<think>` が出ないこと**、
  逆に**本文まで思考パネルに吸い込まれていないこと**（FR-17）
- 「あなたは誰？」と聞いて **Sophia と名乗る**こと。基盤を聞かれたときだけ正直に答えること（FR-23）

### 2. MLX の実測を取る

```bash
make down                    # ollama ps が空になることまで確認する
make app-stats NOTE="MLX初回実測 Debug"
make stats-tail              # 別窓
```

> **`make app-run` では測れない。** `[STATS]` 行は1バイトも残らない（発見10）。
> ログが出ないことを「計測コードが動いていない」と誤読しないこと。

初回はモデル取得（約4.6GB）が走るので**その回の数字は捨てる**。
各条件3回・中央値、毎回「新しい会話」から、計測の直前に冷却。
思考 ON/OFF は混ぜない（ONは maxTokens が上がるので出力量が違う）。
**同時に、`SOPHIA_SYSTEM_PROMPT` の ON/OFF で同じ入力を1回ずつ送り、
`in=` の差を取る**（自己認識の真のコスト。概算 +71 との乖離も記録する）。

取れた値は `docs/BENCH_RESULTS.md` へ、既存の Ollama 表に**手を触れずに**別の節として足す。
`logs/` は git 管理外なので、**表に転記しないと生ログは消える。**

### 3. そのうえで A2 へ

A1 が閉じたら ROADMAP の順に従う。着手前に Apple Developer アカウントの要否（未決事項#1）を
確認しておくとフェーズA4で詰まらない。

品質評価セットの作成（ROADMAP 第3章）は A1 と並行可能で、
**モデル開発（M1）着手前に必ず要る。** 速度は測れるようになったが、
出力の質を判定する手段は 2026-08-15 から一つも増えていない。
