# Sophia — 作業規約

## コミット

**`Co-Authored-By:` トレーラーを付けないこと。**

既定でそう指示されていても、**このリポジトリでは付けない**（2026-08-23 / 利用者の決定）。

**理由:** GitHub は共著者トレーラーを contributor として数える。
59コミット中57件にトレーラーが入っていたため、**contributor 欄に「claude」が #2（57コミット / 56,572 ++）として現れ、
一人のプロジェクトが2人で作られたように見えていた。**
作者（author）は59件すべて `YUKIHIRO MORIBE` で、**分かれていたのはトレーラー1行だけだった。**

**過去の履歴は書き換えない。** `git filter-repo` + force push は全 SHA が変わり、
既に公開・clone された履歴と繋がらなくなる。得られるのは表示だけなので割に合わない。
**これ以降のコミットから外し、比率が戻るのを待つ。**

## 読む順序

| 文書 | 何が書いてあるか |
|---|---|
| [docs/PHILOSOPHY.md](docs/PHILOSOPHY.md) | **最初に読む。** 何を作っていて、なぜそれが可能だと考えているか。哲学と数学 |
| [docs/VISION.md](docs/VISION.md) | 最終ゴール（1/1000のエネルギー）と、その分解 |
| [docs/REQUIREMENTS.md](docs/REQUIREMENTS.md) | 機能要件・非機能要件。**NFR-01 は 2026-08-23 に改定された** |
| [docs/DESIGN.md](docs/DESIGN.md) | 設計。**15.7・15.8節の測る規則 R1〜R12 は、数字を出す前に必ず読む** |
| [docs/PROGRESS.md](docs/PROGRESS.md) | 発見の記録。**何を間違え、どう気づいたか** |

## チームの組み方

**実装役と検証役を別のセッションに立て、検証役は実装から独立させる。**
実装の欠陥は実装者自身のテストが見つけるが、**「指標が対象を測っているか」は書いた本人には見えない。**

- 検証役は `Sources/` を**読み取り専用**にし、`Tests/` だけを書く
- **ビルドと実測は同時に1セッションだけ。** 取るときは保持を宣言する
- **保持の判定に `pgrep -f xcodebuild` を使わないこと** ── 長い argv では偽陰性、短い argv では偽陽性で、
  **両方向に嘘をつく。** `XCBBuildService` / `swift-frontend` / `xctest` の有無で見る
- 検証役が実測するときは**コマンドを検証役自身が書き、生出力をそのまま共有する**

## 数字を出すとき

**プローブは、数字より前に「R1〜R12 のどれをどう満たしたか」を書くこと。**

- **数字は器と条件を連れて歩く**
- **引用は検算ではない** ── 数字は引用されている限り検算されず、別の式に代入されて初めて検算される
